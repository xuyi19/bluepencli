# ==========================================================
# 蓝笔申论 BluePencil · 后端依赖
# 安装：pip install -r requirements.txt
# ==========================================================

# ---------- Web 框架 ----------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# ---------- 数据库（SQLite 异步，零配置；无需安装任何数据库服务） ----------
sqlalchemy>=2.0.30,<2.1
aiosqlite>=0.20

# ---------- 配置与数据校验 ----------
pydantic>=2.9
pydantic-settings>=2.5

# ---------- LLM 调用（OpenAI 协议兼容：DeepSeek / 通义 / 智谱 / OpenRouter 等） ----------
httpx>=0.27

# ---------- 日志 ----------
loguru>=0.7

# ---------- 测试（可选） ----------
pytest>=8.0
pytest-asyncio>=0.24
