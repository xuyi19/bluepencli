// ───────────────────────────── 空实现（stub）─────────────────────────────
// 公开构建时，vite 的 @private-standards 别名指向本文件。
//
// 为什么需要它：私有卷的采分点标准同样是敏感内容（写出采分点等于泄题），
// 必须和私有卷正文一起隔离。构建期别名把 import 整体换成这个空对象，
// 「公开包里不含私有标准」就成了构建事实，而不是靠人记得别打包。
//
// ⚠️ 导出名必须与真实实现（data/standards-private/index.js）完全一致，否则公开构建报错。

export const PRIVATE_STANDARDS = {}
