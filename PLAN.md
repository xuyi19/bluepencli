# 蓝笔申论 · 开发计划与现状

> 最后更新：2026-09-21 ｜ 当前版本 **v0.14.0** ｜ 对照内部《蓝笔申论完整方案 · v4.0》的 V1–V4 路线
>
> 本文只回答两件事：**已经做完了什么**、**还差什么**。版本逐条改动见 [`CHANGELOG.md`](CHANGELOG.md)，架构与用法见 [`README.md`](README.md)。

---

## 〇、一页速览

**现状**：一个可分发、可复算的申论 AI 批改工具。五位老师独立阅卷 → 分歧复核 → 加权合议，
评分有采分点锚点、有纯代码校验兜底；支持网站版 / 单文件版 / 桌面版三种产物，
桌面版（Tauri 2，**单文件 8 MB**）可以零配置发给别人双击即用。

**一句话卡点**：**还没用真实 Key 跑过一次端到端批改**。所有验证至今都在代码层与接口层
（假 LLM 把链路验穿了，但内容质量与真实成本没测过）。

**路线图进度**：

| 方案版本 | 主题 | 用户价值 | 状态 | 对应产品版本 |
|---|---|---|---|---|
| V1 | 最小可用 | 能用 | **已完成** | v0.1.0 – v0.5.0 |
| V2 | 私有题库分发 + 微信引流 | 可分发 | **代码部分已完成**（仅剩需真 Key 才能做的题库校准） | v0.6.0 – v0.11.0 |
| V3 | 训练闭环 | 可训练 | **进行中**（错误类型口径 + 复盘卡 + 错题本已完成） | v0.12.0 – v0.13.0 |
| V4 | 完整版（省考导入 + 考场模式） | 完整 | 未开始 | — |

---

## 一、已经完成

### 1.1 阅卷内核（V1 的核心，也是与同类产品的分界）

- **五位真实名师 Agent**（袁东 / 周泰然 / 白鹭 / Kiwi / 李崇立），讲义原文内置，
  每人**评审侧重与权重不同**——这是"分数会真的分歧"的前提，否则圆桌只是演戏
- **圆桌调度**：并行阅卷 → 分歧检测（差值 > 15%）→ **仅真分歧才辩论** → 加权合议
- 支持 **1~5 人任意组合**，单人 / 双人 / 圆桌三种用法；深度模式按需注入讲义原文
- **逐句批注**：按老师各自颜色标在作答原文上，同一句被多人标中时分段着色，
  悬停看全部意见；每位老师另给一段「如果只改一处先改哪里」

### 1.2 评分内核（v0.7.0 补齐，可复算的那一半）

拆成五层，**能确定性算出来的绝不交给模型**：

| 层 | 由谁负责 | 状态 |
|---|---|---|
| ① 题目标准层：采分点（分值 · 材料依据 · 判据关键词） | 预解析 / 人工精校，固定复用 | 已完成 |
| ② 模型阅卷层 | 五位老师各自独立 | 已完成 |
| ③ 纯代码校验层：字数 / 格式 / 结构 / 重复照抄 | `utils/grading/rules.js`，零依赖 | 已完成 |
| ④ 合议层 | 分歧检测 → 辩论 → 加权合议 | 已完成 |
| ⑤ 训练层 | — | **未开始** |

- 有标准走标准、**无标准裸判**，不会因为一道题没录标准就罢工
- 客观扣分**封顶 20%**、只作旁证、**不改写 AI 分数**；结论同时注入辩论与合议，避免议而不提
- 采分点标准**按公开 / 私有分层**：公开在 `data/standards/`，私有在 `data/standards-private/`
  （已 gitignore，走 `@private-standards` 别名）——**写出采分点等于泄题**
- 练习页新增「客观校验」「采分点对照」两块面板
- 批量预解析工具 `.tools/standards/gen_standards.mjs`（`--mock` 无 Key 也能跑通）

### 1.3 题库与内容

- **33 套国考真题**（2010–2024）入库，含完整给定资料与参考答案，按套懒加载
- 真题流水线：PDF → 分题 → 结构化 → **六道质量闸门**（材料非空 / 题干要求分值字数非空 /
  分值合计 100 / 答案齐全 / schema），33 套全过
- **按年份分层**：≤2021 公开（24 套 / 119 题，随包分发）；≥2022 私有（9 套 / 45 题，定向分发）
- 内置 **15 道仿真题**（五种题型各 3 道，材料为按真题风格自编，界面明确标注"仿真"）
- 内置 **31 篇官媒时评**（约 9.5 万字，覆盖 9 个主题）
- 每日一练：按本地日期散列，**同一天恒定、相邻日期不连号**；从题库带入题目不丢字段

### 1.4 分发能力（V2 的框架部分）

- **三种产物**：网站版 `dist/`（部署用）、单文件 HTML（离线双击开）、
  **桌面版单文件 exe（8 MB，双击即用）**
- 桌面版把界面与 API 收在**同一进程**（Tauri 内嵌前端 + Rust 网关），
  跨域问题从根上消失；exe 同级放 `config.json` 即零配置
- **私有题库包（.bpq）**：作者侧导出 → 题库页导入 → 拆单题写库；发包前用
  `.tools/verify-bpq.mjs`（与前端同一份算法）自检
- **导入两个入口，一条逻辑**（v0.11.0）：把 `.bpq` **拖进窗口任意位置**，或点「导入题库包」选文件。
  监听挂在 `window` 上（用户拖文件不会瞄准某个小按钮），落点限制靠扩展名过滤；
  两条入口共用同一段导入函数 —— 否则迟早有一条忘了处理加密包的口令
- **题库包加密 + 签名**（v0.10.0）：正文走 **AES-256-GCM**（密钥由分发口令派生，
  口令与包**分开两条路**给，所以包外流也读不出内容）；外层走 **ECDSA P-256 签名**
  （证明"确实是作者发的"，且签名覆盖水印，改水印也验不过 —— 「泄露可溯源」靠这条）。
  导入时**先验签再解密**，伪造包直接拒掉。v1 明文包继续兼容
- **发放工具**（v0.11.0，作者侧 `issue.mjs`）：
  - **口令批次**：同批人共用一个口令，换批必须换口令。新批次口令**随机生成**，
    显式复用别的批次的口令会被**直接拒绝**——手工发最容易图省事一直用同一个口令，
    代价是一人泄露、全批人的包都成了公开资源
  - **发放台账**：记「发给谁 / 哪批口令 / 哪个文件 / **文件 sha256**」。
    哈希是关键——事后靠它确认"流转的就是我发的那份"，再配合包内水印追到人；
    `--verify-ledger` 重算哈希核对，包被改过就报不一致
  - **按人改水印**：一份明文包发 N 个人 → N 份水印各异的包，且各自换 IV 重新加密
- **产物自证版本**：构建注入 `app-version` meta（接口 `health` 也回报版本）；
  各产物通道目录里只留最新一版，往期进**该通道自己的** `历史版本/`
- **发包体检**：`.tools/check_release_private.py` 递归清点产物，报「exam chunk 数 /
  有无私有卷 / 功能指纹」——**让文件自己回答，不靠记忆**
- **分发台账**：`release/README.md`（不进库），逐版本记「有什么功能 / 哪个包能发给别人」

### 1.5 稳定性护栏

**现行（Tauri 2）**：

- **产物必须真内嵌前端**：`cargo build --release` 出来的 exe 会去连 `devUrl`，
  它照样起窗口、进程照样活着 —— 用"进程存活"判断必然全绿，而屏幕上是错误页。
  护栏 `.tools/probe-tauri-render.mjs` 断言 URL 必须是 `tauri.localhost`（26 项）
- **入口 HTML 一律 `no-cache`**：避免"升级了却看到旧界面"，带哈希的静态资源长缓存
- **单实例**：两个实例抢 WebView2 的 user data 目录，会让第二个窗口初始化不出页面

**旧版（PyInstaller，已退场）为什么那样设计** —— 保留作记录，解释当时踩过什么：

- 实例复用必须比对版本：旧版占着 8765 时，双击新版 exe 会打开旧实例并自己退出，
  症状与"解压错包"一模一样（v0.8.1 的根因）
- 关掉浏览器页面就退出进程（v0.9.0）：只认"关闭"不认"心跳"——后台标签的定时器会被浏览器节流，
  拿它当"还活着"会误杀；刷新等于「先 bye 后 hello」，留 6 秒窗口重新注册。
  **Tauri 版不需要它**：窗口就是进程，关窗口即退出

### 1.6 工程与文档

- 后端：FastAPI + SQLAlchemy async + SQLite（可一键关闭持久化），只做**转发 / 并发 / 记账**
- 前端：Vue3 + Vite + Tailwind，自然有机风，颜色全走语义 token
- 测试：**pytest 22 项** + 硬规则 24 组 + 采分点 17 组 + 可信度 36 项 + 题库包 20 组
  + 发放工具 24 组 + 9 条路由截图 + 引流 / 面板 / 方格纸 / 跨运行时 / 拖拽导入 / Tauri 渲染等专项探针
- 文档：`README.md`、`CHANGELOG.md`、`docs/上线部署分析.md`、`docs/开发记录.md`、`docs/真题数据说明.md`
- 作者署名唯一来源 `data/author.js`；核心源文件带作者注释头（`.tools/add_watermark.py`）

---

## 二、还没完成

### 2.1 阻塞项（不解决，后面都白搭）

- [ ] **填入可用 API Key，跑通一次真实批改**。目前所有验证都在代码层与接口层，
      端到端批改链路**一次都没实际跑过**——内容是编的、分数是假的
      　→ 跑完后用 `.tools/report-cost.py` 取数（读后端 SQLite 的
      `grading_tasks` + `llm_call_logs`；**桌面版的库在 `release/<版本>/data/`，
      跟 `backend/data/` 不是一个**，脚本会自动挑最近写入的那个）。
      建议首测题：内置仿真 `builtin-q-01`（20 分 / 250 字 / **唯一一道有人工精校标准**的题），
      题干「概括 Y 省推进数字乡村建设的主要做法」
- [ ] **实测真实成本**：三师圆桌 + 深度模式单次预估 ¥0.12，五师约 ¥0.22，需要实测校准
- [ ] **实测 Token 是否真的降到 V1 目标**（方案验收线：常规模式单题 ≤ 2500 token）

### 2.2 V2 收尾（"可分发"还差这几步）

- [x] **导入 UI 支持拖拽**（v0.11.0）：把 `.bpq` 拖进窗口任意位置即可，与"选文件"共用一段导入逻辑
- [ ] **40 道国考题库校准**（当前仅 1 道人工精校样板 + 批量预解析产物）
- [x] **分发记录管理**（v0.11.0）：`issue.mjs` 记发放台账（谁 / 哪批口令 / 哪个文件 / **sha256**），
      `--verify-ledger` 可重算哈希核对
- [x] **分销口令的管理**（v0.11.0）：口令**批次**化，新批次随机生成，
      显式复用别的批次的口令会被拒（`--allow-reuse` 才能放行）

> 已完成的两项（加密、签名）见 1.4 —— 它们原本也在这一节，
> v0.10.0 做完了就挪上去了。
> **V2 只剩「40 道校准」一项，而它必须先有真 Key 才能做**（要用真实批改结果对比
> "有标准 / 无标准"的分数稳定性）。所以 V2 的代码部分到此收齐，
> 剩下的卡在同一个前提上：**先跑一次真实批改**。

### 2.3 V3 训练闭环（进行中）

- [x] **错误类型统一口径**（v0.12.0）：12 个固定分类 + 自由文本归一化 + prompt 改用固定 id。
  这是 V3 其余几项的地基 —— 自由文本聚不起来，「采分词缺失」「漏点」「要点不全」
  是同一件事却被算成三类
- [x] **复盘卡**（v0.12.0）：把一份批改压成「下次动笔前先改这 N 处」，
  按**共识优先**排序（几位老师各自独立提到 = 硬伤），附动笔前自检清单
- [x] **错题本**（v0.13.0）：按「栽在几份记录里」排序的列表页，反复出现的排前面、
      单发的收进折叠区；只有 1 份记录时明说"还不算反复"，不假装有结论
- [x] **再练一题**（v0.13.3）：按短板从题库推下一题，分「同题型对症」与「跨题型通病」两档；
      判据不足时**不给推荐**。为此批改记录开始存题型（老记录推不了，会说明原因）
- [ ] 能力画像（按五种题型出雷达图——`StatsView` 的雷达图是雏形，未接结构化评分数据）
- [ ] 结果分层展示（总览 → 为什么 → 老师意见 → 辩论记录）—— 复盘卡已是"总览"那一层的开头
- [ ] 评分可信度指标（采分点覆盖率、规则命中数、分歧幅度）

### 2.4 V4 完整版（整体未开始）

- [ ] 用户导入工具链（Markdown / PDF → 题目）
- [ ] 预解析向导 + 本地校准工作台
- [ ] 省考题型模板
- [ ] 真题模式 / 训练模式 / **考场模式**
- [ ] 大作文档级约束、论证诊断

### 2.5 上线（详见 `docs/上线部署分析.md`）

- [ ] **访问控制**（共享访问码 / OAuth）
- [ ] **配额限流**——必须**按批改次数**限，不能按请求次数限
      （一次圆桌 = 3~7 次 LLM 调用）
- [ ] 部署到服务器（Nginx 反代记得 `proxy_buffering off`，否则流式会被攒起来）
- [ ] 成本上限与熔断（防止被刷爆 Key）

### 2.6 零散待办

- [ ] 老师档案页的 Markdown 渲染（现在是纯文本，标题表格都是原样字符）
- [ ] 批改结果导出（Word / PDF）
- [ ] OCR 识图（已确认暂不做）
- [ ] 手写板（已确认优先级最低）
- [ ] 交流群二维码每 7 天过期换图（覆盖 `src/assets/wechat/group.jpg` + 改 `groupExpireAt` 后重打包）

---

## 三、建议的下一步

1. **先用真 Key 批一次**（最高优先级，哪怕只批一道仿真题）。它同时验证成本、Token、
   内容质量三件事，而这三件事现在全是空白——再往后做功能都是在没校准的地基上盖楼。
2. 校准 **3~5 道题的采分点标准**，用真实批改结果对比"有标准 / 无标准"的分数稳定性
   （V2 的最后一项，同样要先有真 Key）。
3. 进 **V3**（v0.12.0 起步、v0.13.0 补齐错题本）：错误类型口径、复盘卡、
   错题本列表页都已完成，接着做**再练一题**（按错题本里的短板推荐下一题），
   能力画像放到有足够数据之后。
   —— 这是**当前唯一不依赖真 Key 就能推进**的方向。

> V2 的代码部分已在 v0.11.0 收齐（加密签名、拖拽导入、口令批次、发放台账）。
> 往后每加一个功能，都应当先问一句：**它是不是又在没校准的地基上盖楼？**

---

## 四、产品版本 ←→ 方案版本对照

| 产品版本 | 主要交付 | 对应方案阶段 |
|---|---|---|
| v0.1.0 | 五师阅卷 / 圆桌合议 / 双通道 / 两种分发形态 | V1 起步 |
| v0.2.0 | 界面重构（自然有机风）、题库 15 题、每日一练、方格纸 | V1 |
| v0.3.0 – v0.4.0 | 首页入口、更新日志文档化、产物带版本号 | V1 |
| v0.5.0 | 33 套国考真题入库 + 真题流水线 | V1 收尾 |
| v0.6.0 | 作者署名体系、.bpq 题库包、**真题分层** | **V2 起步** |
| v0.7.0 | **评分内核补齐**：采分点标准层 + 硬规则引擎 | V1 的验收项回填 |
| v0.8.0 | 微信引流（四处入口 + 群码降级）、分发台账 | V2 |
| v0.8.1 | 产物自证版本、修「升级后还是旧界面」 | V2 稳定性 |
| v0.9.0 | 桌面版**关掉页面就退出进程**（消掉残留实例这个状态） | V2 稳定性 |
| v0.10.0 | 题库包**加密 + 签名**：口令分开给、作者身份可验 | **V2 功能面收齐** |
| v0.11.0 | 题库包**拖拽导入** + 发放**口令批次 / 台账** | **V2 代码部分完成** |
| v0.12.0 | **错误类型口径 + 复盘卡**（共识优先排序） | **V3 起步** |
| v0.12.1 | 修「更新日志页读不了」（解析器只认最小约定，页面能开≠能读） | V3 期间修复 |
| v0.12.2 | 修「点侧边栏日志没反应」（入口 61×28px、间隙 6px，点偏就落空） | V3 期间修复 |
| v0.13.0 | **错题本列表页**（按「栽在几份记录里」排，不按被提次数） | **V3** |
| v0.13.1 | 修「升级后点导航没反应」（懒加载分片 404 → 路由中止跳转） | V3 期间修复 |
| v0.13.2 | **路由改静态导入**，根治「点导航没反应」；入口加版本自检 | V3 期间修复 |
| v0.13.3 | **「再练一题」**（按短板推下一题，定向/泛化两档，没依据不硬推） | **V3** |

---

## 五、技术路线变更记录

| 阶段 | 决策 | 原因 |
|---|---|---|
| 起初 | Tauri 2 + Rust | 作品集含金量最高 |
| 后改 | 纯前端 + 单文件分发 | 避开 4~6GB MSVC 环境，且"发给别人就能用"更重要 |
| 再改 | 前后端分离 | 对齐既有工程结构，后端解决 CORS 并承担网关职责 |
| **定稿** | **三种产物并存** | 网站版保住工程规范与上线能力；桌面版 zip 是"发给别人双击即用"的主力；单文件 HTML 作轻量备选 |
| **定稿** | **Open Core 分层** | 公开层（≤2021）随包分发保住传播力；私有层（≥2022）定向分发保住资产价值 |

> 两套定稿都不再变动。若后续要改，先回到本文档更新「变更记录」，再动代码。

---

## 六、本机归档状态

`release/` 是**本机归档区**，不进版本库（`.gitignore` 整目录忽略）：

```
release/
├── 蓝笔申论-单文件版-v0.13.3.html    最新
├── 蓝笔申论-桌面版-v0.13.3.zip       最新（约 24 MB，可分发）
├── 蓝笔申论-桌面版-v0.13.3/          同名解压目录，本机试跑用
├── 历史版本/                          往期产物，只挪不删
├── 私有题库/                          export_bpq.py 出的明文包 + issue.mjs 发出的加密包
│                                     （含私有卷，绝不分发；明文包仅本机可见）
├── 发放台账.json / 发放台账.md        发给谁 / 哪批口令 / 文件 sha256（不进库）
└── 版本说明.md                        分发台账：哪个包能发给别人（不进库）
```

> **v0.4.0 / v0.5.0 的桌面版包是真题分层之前打的，内含 2022 年起私有卷**，
> 属泄漏风险，已清理，不在归档之列。任何产物在分发前都应跑一遍
> `backend/.venv/Scripts/python.exe .tools/check_release_private.py`。
