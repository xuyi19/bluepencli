# 更新日志

蓝笔申论（BluePencil）的版本改动都记在这一份文件里，**新的版本写在最上面**。

> 网页端的「更新日志」页**直接读这份文件**，所以改完保存就等于改好了页面，
> 不需要再同步任何前端代码；打包脚本也从这里取版本号给产物命名。

## 写法约定（解析器依赖，改格式前先看这里）

- 版本标题固定一行：`## vX.Y.Z · YYYY-MM-DD · 一句话标题`
- 改动分节写：`### 新增` / `### 优化` / `### 变更` / `### 修复` / `### 测试` / `### 删除`，
  每条以 `- ` 开头
- 一条写不完就**折行续写**（缩进两格接着写），页面会把它接成一句。
  接缝的补空格规则：中文之间**不补**（不然句中会凭空多一个空格），
  接到英文单词或符号（`/`、数字）时才补 —— 所以放心折
- 给某条挂子项：**缩进两格再写 `- `**，父条目末尾用「：」收口
- 写附注（比如"某个旧包有风险"）用 `>` 引用，页面渲染成单独一行小字
- 行内 `代码` / **加粗** / *斜体* 都支持，会渲染成元素，不会把记号原样显示
- 脚本只依赖第一条：版本号取**最上面那一版**。`frontend/scripts/publish-single.mjs` 与
  `backend/build_desktop.py` 都按这条规则读，读不到会直接报错停下，不会静默出个错包
- ⚠️ **改完跑 `node .tools/test-changelog.mjs`**。它盯的是「条目有没有丢、
  句子有没有被切断、记号有没有漏出去」—— 页面**能打开**不等于**能读**，
  v0.12.1 就是栽在这个区别上。

---

## v0.12.2 · 2026-09-17 · 修「点侧边栏日志没反应」

### 修复
- **侧边栏的「日志」入口点不动**。它和 GitHub / Gitee 挤在同一行三等分，
  每个只有 61×28px、间隙只有 6px —— 鼠标或触控板稍偏一点就落进缝里，
  症状是"点了没反应"，很容易被当成"页面坏了"。日志是会主动去翻的入口，
  现在提升成导航里正常尺寸的条目（199×40px）；底部只留 GitHub / Gitee 两个胶囊，
  它们也因此变宽了。
- 排查时先排除了产物问题：v0.12.1 的 `dist` 与桌面版内嵌 `web` **逐文件一致**
  （assets 都是 54 个、`index.html` 是同一份），没有缺失 chunk；
  在真实浏览器里点它**也能跳转**，页面渲染出 14 版 118 条。
  所以问题不在"发错了包"，而在**点击目标太小**这个纯可用性缺陷上。

### 测试
- 新增 `probe-nav-click.mjs`：用真实 Chrome 的**鼠标事件**逐个点侧边栏入口
  （走坐标命中测试，而不是 `element.click()`），看 hash 变没变、目标页渲染没渲染，
  并量出每个链接的**实际尺寸**，小于 40×32 直接报出来。
  为什么不用 `element.click()`：它把事件直接派给元素、**绕过遮挡**，
  被盖住或被挤出视口外的入口照样"成功"，测了等于没测。
- 这次漏检的根因值得记一笔：`probe-changelog.mjs` 是**直接访问** `#/changelog` 的，
  把"用户从侧边栏点进来"这条真实路径整个跳过了 ——
  **能渲染不等于点得到**。新探针补的正是这一段。

---

## v0.12.1 · 2026-09-17 · 修「更新日志页读不了」

### 修复
- **更新日志页把 Markdown 原样显示，长条目还被截断**（v0.8.1 起逐渐变严重）。
  页面用的解析器只认上面那段最小约定（版本行 / 分组 / **单行**条目），
  而这份文档从 v0.8.1 起越写越按 GitHub 风格来，于是三件事同时发生：
  - 104 条里 **63 条**把加粗记号、行内代码记号当普通文字吐出来；
  - **折行的后半句整段消失**（v0.12.0 第一条断在「分类混乱 /」，后面全没了）；
  - **缩进子条目一条没渲染**（v0.11.0 里 issue.mjs 的三个子项看不见）。
  最难受的是页面**没崩**：版本数、顺序、「每版都有条目」全部对得上，
  于是所有「能打开吗」的检查都放行了 —— 问题是它读不了。
- 解析器拆成 `frontend/src/data/changelog-parse.js`（纯函数，不依赖 Vite 的 `?raw`），
  让单测 import 的是**页面在跑的那份代码**。留在 `changelog.js` 里的话，
  测试只能把正则复制一份过去 —— 而复制的那份永远是对的，页面照旧能坏。
- 现在支持：**折行续写**（中文之间不补空格，接英文才补）、**缩进子条目**、
  `>` **引用附注**（单独一行小字，不糊进正文）、行内 `代码` / **加粗** / *斜体*。
  渲染走**片段化**而不是 `v-html`：文档是自己写的，但「把 Markdown 当 HTML 解析」
  这个口子不该开。
- 行内解析踩了一个顺序坑：**不能**先把代码段切下来再各自找加粗 ——
  加粗跨过代码段时（像「发放工具 issue.mjs」这种写法），切开后两边各剩半个记号；
  反过来整行匹配又会被代码里的 `*`（如 `/wechat/*.jpg`）当边界而失配。
  改成用占位符把代码**挖走**（不是切开）再整体匹配，最后还原。
- `probe-site-links.mjs` 修掉一条**失效了六个版本**的断言：它还在首页找「更新日志」胶囊，
  可那个入口 v0.6.0 就挪到侧边栏底部了（且侧边栏上的字是「日志」），
  于是这条断言从那时起一直在报假失败 —— 这条探针要起 dev server 才跑，
  没人跑就没人发现。改为窄屏下打开抽屉、断言入口真的够得着。

### 新增
- `### 测试` 这一节在页面上有了自己的颜色。原来它落到默认灰样式，
  跟「认不出来的分组」长得一样。

### 测试
- 新增 `test-changelog.mjs`（23 项）。看的不是「渲染了几个版本」，而是**内容完不完整**：
  条目有没有丢、句子有没有被切在半路、记号有没有漏出去、
  **续行拼接是否真的生效**（直接钉住「整句照抄」「私钥 gitignore」这些折行处的词）。
- `probe-site-links.mjs` 增加四条**可读性**断言：记号渲染成了元素、折行后半句在、子条目在。

---

## v0.12.0 · 2026-09-17 · 错误类型统一口径 + 复盘卡（V3 起步）

### 新增
- **错误类型表**（`data/error-taxonomy.js`）：12 个固定分类，如「要点遗漏 / 拆点 / 分类混乱 /
  整句照抄 / 概括失真 / 空话套话 / 形式不应题 / 结构失当 / 字数失当 / 格式体例 / 表述不规范」。
  每类带一句**能照着做的**「动笔前自检」，复盘卡的检查清单直接用它生成。
- **自由文本归一化**：老数据里模型写的 `type` 是自由文本（「采分词缺失」「漏点」「要点不全」
  其实是同一件事）。归一化函数把它们收进同一格，认不出来的一律归「其他」——
  **宁可漏归一类也不硬塞**，错塞会让复习重点指错方向，比不分类更误导。
- **prompt 改用固定口径**：新批改的 `type` 从表里挑 id，一句话犯两种毛病时写成
  `copy-raw,over-generalize`。这样跨记录统计才立得住。
- **复盘卡**（练习页结果区 + 历史批改详情页）：把一份批改压成「下次动笔前先改这 N 处」，
  每处给出原文引用、改法、扣分合计，最后附一张动笔前自检清单。
- **短板统计**（`buildWeaknessProfile`，数据层就绪）：跨记录按类型聚合，
  排序按「栽在几份记录里」而不是「被说了几句」——同一份答案里同一位老师反复说同一件事，
  不该比连续三次练习都掉进同一个坑更值得复习。

### 优化
- 复盘卡排序按**共识优先**：几位老师**各自独立**提到同一类问题 = 硬伤；
  只有一位提到可能只是他的侧重点。共识相同时看扣分，最后看出现次数。
  单人模式下「共识」这件事不成立，界面会**如实改口径**（只说「1 位老师提到」），
  不拿「老师都提到了」唬人。
- 排序不需要再调一次模型：意见已经在批改结果里，再调一次只是换个说法，
  还会引入「两处说法不一致」。这里是纯代码聚合，可复算。
- 侧边栏分组名与条目名不再同名（分组「复盘」下的条目「复盘」→「历史批改」），
  原来侧边栏会出现两行一模一样的字，看着像重复项。

### 修复
- `skills.js` 的 prompt 里混进了反引号，把外层模板字符串截断，构建直接失败
  （`vite-dynamic-import-vars` 报"无法解析"）。prompt 是一整个模板字面量，
  里面出现反引号必须换掉。
- 归一化规则顺序钉住两处易混：`展开不足` 里含 `不足`，必须排在「字数失当」之前，
  否则「分论点展开不足」会被判成字数问题；「形式不应题」与「结构」类关键词可能同时出现
  （如"形式不应题、结构也松散"），应题类必须排在结构类之前才不会被抢走主类。

### 测试
- 新增 `test-review-card.mjs`（37 项）：归一化 37 例（含 4 组反例专测）、
  复合 id 写法、共识排序、单人模式口径、`其他` 不上榜、去重与条数上限、跨记录短板统计。
- `e2e-grade.mjs` 增加复盘卡断言：用假 LLM 跑完整批改链路，
  验证三位老师的自由文本批注（「采分词缺失」「空泛表态」）真的聚成了两类并命中共识标记。

---

## v0.11.0 · 2026-09-17 · 拖拽导入题库包 + 发放批次台账（V2 收尾完成）

### 新增
- **题库包拖拽导入**：把 `.bpq` 文件拖进窗口任意位置就能导入，不用再去点按钮找文件。
  拖拽期间整页给一层提示，松手即入。加密包照旧会弹口令框——
  拖拽和"选文件"走的是**同一段导入逻辑**，不会只有一条路支持加密包。
  监听挂在 `window` 上而不是某个框上：用户拖文件时不会瞄准某个小元素，
  落点在哪都得接住；落点限制靠扩展名过滤（`.bpq` / `.json`）而不是靠绑小元素。
- **发放工具 `issue.mjs`**（作者专用）：
  - **口令批次**：同一批人共用一个口令，换一批人必须换口令。新批次的口令一律
    **随机生成**（避开 0/O、1/I 这类口头传递会听错的字符），
    并且显式想复用别的批次的口令时会**直接拒绝**（`--allow-reuse` 才能放行）——
    "图省事一直用同一个口令"的代价是一人泄露、全批人的包都成了公开资源。
  - **发放台账**（`release/发放台账.{json,md}`，不进版本库）：记下发给谁、哪一批口令、
    哪个文件，以及**文件当时的 sha256**。哈希是这里的关键——事后对方手上那份包流转出去，
    能靠哈希确认"流转的就是我发的那份"，再配合包内的使用者水印追到人。
    `--verify-ledger` 会重算哈希核对，包被改过就报不一致。
  - **按人改水印**：一份明文包 + N 个收件人 → N 份包，**水印各不相同**，
    且每次都重新加密（换 IV），所以同一个包发给两个人，密文也不一样。
- **`seal-core.mjs`**：把"明文包 → 加密包"抽成唯一实现，
  `seal-bpq.mjs`（手工封一个）与 `issue.mjs`（按批次批量发）共用。
  各写一份的话，签名覆盖字段一旦有细微差别，症状是"手工封的能开、批量发的开不了"，极难定位。
- 新增两组真浏览器 / 真数据测试：
  `probe-pack-drop.mjs`（真 Chrome 12 项：提示层显隐 / **划过子元素不闪** /
  纯文本拖拽不误触发 / 松手 .bpq 真的走完导入 / 不支持的类型无副作用）；
  `test-issue.mjs`（24 项：换批换口令的约束 / 同批次同口令但水印各异 /
  台账哈希对得上 / 包被改后核对报错）。

### 修复
- `issue.mjs` 台账时间改用**本地时间**：原来走 `toISOString()` 是 UTC，
  本机 +8，凌晨发的包（本地 07:00 = UTC 前一天 23:00）会被记成前一天。

### 变更
- `.gitignore` 增加 `.tools/exams/batches.json`（存着口令原文，进库等于公开私有题库包）。

---

## v0.10.0 · 2026-09-17 · 题库包加密 + 签名（V2 收尾）

### 新增
- **题库包加密（.bpq v2）**：AES-256-GCM，密钥由分发口令经 PBKDF2-SHA256（20 万次）派生。
  正文（材料与参考答案）加密后放进 `crypto` 段，**包本身外流也读不出内容**。
  口令由作者**另外告知**——和包走两条路，一起发等于没加密。
- **ECDSA P-256 签名**：证明"这个包确实是作者签发的"。
  **GCM 的完整性不等于身份认证**：知道口令的人都能造出合法密文，签名才能把作者身份钉死。
  签名覆盖水印字段，所以改水印也会验不过——「泄露可溯源」这句话到这才有依据。
  验签排在解密**之前**：伪造的包直接拒掉，不必白跑一次 PBKDF2。
- **作者侧两个工具**：
  `bpq-keygen.mjs` 生成密钥对（公钥自动写进 `frontend/src/bpq/pubkey.js`，私钥 gitignore）；
  `seal-bpq.mjs` 把明文包封装成加密包，自带"回读解密"自检与 `--check` 复核模式。
- **导入时弹口令框**：题库页导入加密包 → 遮字输入框 → 最多试 3 次。
  `ToastHost` 因此多了 `toast.askPassword`（仍是自实现，不引 UI 库）。
- 新增两组测试：
  `test-bpq-crypto.mjs`（20 项：口令错 / 密文被改 / **水印被改** / 换成他人公钥 /
  他人私钥冒充 / 老包回归 / 作者命令行通路）；
  `probe-bpq-browser.mjs`（真浏览器 7 项，验**跨运行时**互操作：Node 加密 → 浏览器验签解密）。

### 变更
- **公钥改成数组**（`BPQ_PUBLIC_KEYS`）：轮换密钥时**追加**而不是替换——
  删掉哪一把，之前发出去的包就永久验不过签了（那些包用户手上还在用）。
- `.bpq` 新增 v2 格式，**v1 明文包继续支持**（已发出去的包不能失效）。
  导入时按 `magic` 自动分辨（`BPQ00001` / `BPQ00002`）。
- `verify-bpq.mjs` 支持加密包（`--passphrase`），校验内容按格式分流。

### 优化
- 加密与签名统一走 **WebCrypto**（Node 与浏览器是同一套 API）：后端**零新增依赖**，
  也绕开了"Python 的 ECDSA 默认输出 DER、而 WebCrypto 要 raw(r‖s)"这个必踩的坑
  （踩了的话症状是"验签永远不通过"，且看不出原因）。
- 明文包与加密包文件名区分（`-加密` 后缀）：`release/私有题库/` 里哪个能外发一眼可辨。

### 修复
- `openSealedPack` 传入**空公钥列表**时会静默回退到内置公钥 ——
  于是"程序里压根没有验签公钥"被误报成"签名不通过"，把用户引去怀疑包本身。
  现在明确区分「没传」（用内置）和「显式传空」（就是没有可用公钥）。

---

## v0.9.0 · 2026-09-17 · 桌面版：关掉浏览器页面就自动退出

### 新增
- **桌面版「关页即退」**：浏览器页面全部关掉后，程序自己结束进程（黑窗口随之消失），
  不用再去手动关窗口。此前只认「关黑窗口」，用户关掉页面后普遍以为程序已经退了，
  可进程还在后台占着默认端口 —— 下次双击就命中残留实例，看着像"启动了"，
  其实什么也没重启（升级后打开旧界面的问题，根子就在这）。
  **判定三条同时成立才退**：曾经有过页面会话 + 当前没有活跃会话 + 这个"空"状态
  已持续超过 6 秒。第三条是给**刷新**留的窗口：刷新等于「先 bye 后 hello」，
  新页面在 6 秒内注册回来就把倒计时取消掉。
  **心跳不参与判定**，只用来回收僵尸会话 —— 后台标签页的定时器会被浏览器节流，
  拿它当"页面还活着"的证据会把正常开着的页面误杀。
- **会话端点**：`/api/v1/session/hello`（注册 / 复位倒计时）、`/session/ping`（心跳）、
  `/session/bye`（注销）、`/session/state`（查看现状）。
  `bye` 走 POST 是有意的：页面卸载阶段只有 `navigator.sendBeacon` 的 POST 发得出去，
  普通 `fetch` 会被浏览器掐掉。
- `health` 新增 **`desktop` 字段**：前端据此决定挂不挂这套逻辑。
  网站版 / 单文件版一律不挂 —— 否则某个访客关掉标签页就会把别人的服务杀掉。
- `backend/tests/test_session_watch.py`：**13 项用例**，正反两个方向都钉住 ——
  刷新不误杀、多标签只关一个不误杀、从没开过页面不误杀、崩溃后僵尸会话会回收、
  退出只触发一次。全部直接喂时间戳复算，不真的等 6 秒。

### 变更
- `desktop.py` 改用显式的 `uvicorn.Server`（原先用 `uvicorn.run` 的语法糖），
  这样才能拿到 server 句柄做**优雅退出**：关页后照常走完 lifespan 收尾
  （关掉 httpx 客户端、dispose 数据库连接池）再退，而不是把进程直接砍掉。

### 优化
- 黑窗口提示改为「关掉浏览器页面就会自动退出，也可以直接关掉本窗口」。

---

## v0.8.1 · 2026-09-14 · 产物自证版本 + 修「升级后还是旧界面」

### 修复
- ⚠️ **最要命的一条：升级后双击新 exe，打开的却是旧界面**。`desktop.py` 原本只判断
  「默认端口上跑的是不是 BluePencil」就复用实例、**不比对版本** —— 用户升级后旧窗口
  往往还开着，于是新 exe 命中旧实例、把浏览器指过去、自己退出。现象就是「解压出来
  是旧版的」，而新版其实已经装好了，且新进程的日志一切正常（所以极难自查）。
  现在**只复用版本相同的实例**；版本不同就另起端口，并打印
  「端口 8765 上运行着旧版本 v0.7.0，当前是 v0.8.1，新版本已改用 …:8766」。
- **桌面版升级后打开的仍是旧界面（缓存那半边）**：静态资源由 FastAPI `StaticFiles` 直接托管，
  不带任何 `Cache-Control`，浏览器走启发式缓存把入口 HTML 留住了。
  桌面版每次升级都跑在同一个地址（`127.0.0.1:8765`），于是"解压了新包、看到旧界面"，
  看着像发错包，其实是缓存。现在入口 HTML 一律 `no-cache, must-revalidate`
  （靠 ETag 拿 304，不额外费流量），带内容哈希的 js/css/图片长缓存。

### 新增
- **产物自证版本**：构建时把 `app-version` / `app-release-date` / `app-build-time`
  写进 `index.html` 的 meta（`vite.config.js` 的 `versionStamp` 插件，版本取 CHANGELOG 首版）；
  桌面版另附一个 **`版本信息.txt`**，并在「使用说明.txt」标题带上版本号。
- **`release/历史版本/`**：历史产物挪进子目录，`release/` 根只留最新一版。
  此前多个版本的包平铺在一起、文件名只差一处版本号，随手解压一个就是旧版。

### 优化
- `.tools/probe-wechat.mjs` 支持 `BP_BASE` 环境变量，同一份断言既能验源码
  （dev server 5273），也能验**解压后的桌面版产物**（8765）。
- 新增 `.tools/test-desktop-reuse.py`：盯住上面那条实例复用逻辑的两条分支
  （同版本→复用；版本不同→另起端口且不把用户带去旧界面）。这个坑一旦回归，
  现象只是"界面是旧的"，不会有任何报错。

---

## v0.8.0 · 2026-09-14 · 微信引流 + 版本归档台账

### 新增
- **微信引流入口**：`frontend/src/data/wechat.js` 集中管理二维码与有效期。
  图片走 `import` 引入而不是放 `public/`——**单文件版是双击打开的本地 HTML，
  没有服务器托管 `/wechat/*.jpg`，放 public 会裂图**；走 import 后网站版出独立资源、
  单文件版自动 base64 内联，两条通道都不掉图。
- **四处引流入口**：侧边栏底部「加微信 / 交流群」、首页作者区按钮、设置页「关于」、
  题库页「怎么获取私有题库」。前两处开全局弹层（`components/WeChatHost.vue`，
  支持 ESC 关闭），后两处直接摊开二维码——那两个位置用户本来就在找联系方式，不必多点一次。
- **群二维码到期自动降级**：群码 7 天一轮，`groupExpireAt` 到点后前端自动换成
  「群二维码已过期，加微信拉你进群」。**不展示一张扫了没用的图**，比不展示更伤信任。
  个人微信码长期有效，是主入口；群码只当附加福利。
- **`release/版本说明.md`**：本机维护的分发台账，逐版本记录「有什么功能 / 哪个产物能发给别人」。
  刻意不进版本库——里面写明哪些产物含私有卷，是只给作者看的信息。

### 变更
- **release/ 归档策略：只增不删**。历史版本产物一律保留、互不覆盖（产物名本就带版本号），
  需要回溯"某版当时长什么样"时不用重新构建。
  > v0.4.0 / v0.5.0 的桌面版包是真题分层**之前**打的，内含 2022 年起私有卷，
  > 属泄漏风险，已于 v0.7.0 时清理，不在归档之列。

---

## v0.7.0 · 2026-09-14 · 评分内核补齐：采分点标准层 + 硬规则引擎

### 新增
- **采分点标准层（grading_standard）**：`frontend/src/agents/grading/standard.js` +
  `standardResolver.js`。给题目预先备一份采分点标准（含分值、材料原文依据、判据关键词），
  批改时注入每位老师的提示词，老师有了共同锚点，评分可比、可复算。
  **有标准更好，没有标准照旧裸判**——不会因为一道题没录标准就罢工。
- **硬规则引擎**：`frontend/src/utils/grading/rules.js`。字数 / 标点格式 / 结构分条 /
  重复与照抄四类校验，**纯代码、零依赖、可复算**，不交给模型"感觉"。
  客观扣分默认封顶为满分的 20%，且只作旁证，不直接改写 AI 分数。
- **标准数据分层**：公开卷与仿真题的标准在 `frontend/src/data/standards/`（进仓库）；
  私有卷的标准在 `data/standards-private/`（**.gitignore**，走 `@private-standards` 别名）——
  写出采分点等于泄题，必须和私有卷正文一起隔离。
- **采分点预解析工具**：`.tools/standards/gen_standards.mjs`。批量 LLM 提取采分点与证据串，
  产出落 `data/standards/generated.js`（公开）与 `data/standards-private/index.js`（私有）；
  `--mock` 可在没有 Key 时跑通整条链路，`--limit`/`--ids` 支持小步试跑。
- **练习页两个新面板**：「客观校验」（列出字数/格式/结构/重复问题与改法）与
  「采分点对照」（覆盖率 + 逐点命中/部分/缺失 + 材料依据）。
- 新增两套纯代码单测：`.tools/test-rules.mjs`（24 组断言）、
  `.tools/test-standards.mjs`（17 组断言），以及面板渲染验证 `.tools/probe-standard-panel.mjs`。
- `LICENSE` 之外的采分点样板：`data/standards/public.js` 收录 1 道**人工精校**的样板
  （数字乡村建设），作为格式参照与质量基线。

### 优化
- 硬规则的客观结论会作为「客观事实」注入圆桌辩论与合议的输入，
  避免"字数明显不足""整段照抄材料"这类板上钉钉的问题在合议里无人提及。
- 深度模式的方法论注入顺序调整：先给判断尺度、再给该题采分点锚点。

### 修复
- 仿真题的采分点标准键名统一为**练习页使用的题目 id**（`builtin-q-01`），
  与数据文件里的原始 id（`q-01`）区分——写错键名不会报错，只会静默地永远取不到标准。

---

## v0.6.0 · 2026-09-14 · 作者署名 + 真题分层（公开 2021 及以前，私有 2022 起）

### 新增
- **作者署名体系**：`frontend/src/data/author.js` 是全项目唯一来源（作者 许一 / 邮箱 / GitHub / Gitee /
  开源地址 / 许可）。侧边栏底部、首页页脚、设置页「关于」、浏览器控制台横幅统一从它取；
  `.tools/add_watermark.py` 给 16 个核心源文件打作者注释头，改一次全项目跟着变。
- **侧边栏底部开源入口**：GitHub / Gitee / 更新日志三个入口移到左侧边栏底部常驻
  （原先只在首页页脚，别的页面够不着），侧边栏最下方另有作者署名一行。
- **私有题库包（.bpq）**：作者侧用 `.tools/exams/export_bpq.py` 导出带使用者水印的题库包，
  题库页「导入题库包」一键入库（材料与参考答案都在包里，导入即可练）；
  `.tools/verify-bpq.mjs` 用前端同一份算法在发包前自检。
- **真题分层**：2021 年及以前的国考卷（24 套 / 119 题）随包分发；
  2022 年起的（9 套 / 45 题）为私有资产，**不进版本库、不进分发产物**，由作者定向分发后导入。
- 仓库根 `LICENSE`（AGPL-3.0）。

### 变更
- 真题数据拆成 `frontend/src/data/real-exams/`（公开）与 `real-exams-private/`（私有，已 gitignore）两处；
  只有 `--mode full` / `--mode single-full` 才把私有卷编进去，**默认构建一律不含私有卷** ——
  忘记加 mode 时宁可少东西，也不能把私有资产漏出去。
- 首页页脚改为「作者与开源」信息块（GitHub / Gitee / 日志三个入口已移到侧边栏）。
- 题库页「按来源」筛选新增「私有」；练习页题目区新增来源标签（真题 / 私有 / 仿真 / 自建）。

### 优化
- 题库页说明按公开 / 私有分层重写；未导入私有卷时给出获取途径（作者联系方式可直接复制）。

### 修复
- 练习页与首页把库里所有题目一律标成「自建」，会把导入的私有真题认错来源；
  改为只在缺失时补默认值，不覆盖已有 `kind`。

---

## v0.5.0 · 2026-09-14 · 33 套国考真题入库（2010–2024）

### 新增
- **真题题库**：33 套国考申论卷 / 164 道题（2010–2024，副省级 / 地市级 / 省部级 / 行政执法），
  含完整给定资料与参考答案。题库页可按「真题 / 仿真 / 自建」筛选，卡片标出年份与卷别
- **真题数据流水线**（`.tools/exams/`）：PDF → 分题 → 结构化 → 质量闸门 → 前端数据。
  六道闸门（材料非空 / 题干·要求·分值·字数非空 / 分值合计 100 / 答案齐全 / schema）
  **33 套全部通过**，正文体检 0 处可疑
- 真题**按套懒加载**：题库列表只带摘要，练到哪题才载入该卷正文。
  网站版每套约 15KB（gzip），单文件版自动内联，离线双击仍然可用

### 优化
- 题库页列表按年份从新到旧排列，真题排在仿真题之前（先练近年真题）
- 「参考答案」在题库页按需展开载入，不必提前读完整卷材料
- 题型自动归类覆盖五种题型，「其他」从 42 道降到 0 道

### 修复
- 一题多小问时，「（N分）」与「要求：」之间的**子问题正文被丢弃**（2021 副省级第 3 题）
- 2016 地市级第三题两个小问各自的字数要求与分值被合并后丢失（现各自保留、字数取和）
- 末题要求尾部混入「…卷（解析）」这类答案页眉
- 源 PDF 的 17 处 OCR 错字与引号错位（如 `行政i款`→`行政罚款`、`关于s市`→`关于S市`），
  统一收进 `extract.py` 的 `FIXUPS` 修正表，重跑即复现，不做手工改 JSON

## v0.4.0 · 2026-09-14 · 更新日志文档化，发布产物带版本号

### 新增
- 更新日志改由本文件驱动：网页「更新日志」页直接解析这份 Markdown，改文档即改页面
- 发布产物统一带版本号（`蓝笔申论-单文件版-v0.4.0.html`、`蓝笔申论-桌面版-v0.4.0.zip`）
- 新增 `npm run release:single`，一次完成单文件版构建、改名、清理旧版本

### 优化
- 首页的仓库入口与更新日志入口，从标题旁下移到页面底部，放在配置提示之上

### 变更
- 版本号的唯一来源改为本文件最上面一版，前后端不再各写一份
- `release/` 只保留最新一版产物，重打包时自动清掉旧版本副本

## v0.3.0 · 2026-09-14 · 首页入口 + 更新日志

### 新增
- 更新日志页，按版本记录每轮改动
- 首页 GitHub / Gitee 仓库入口

### 优化
- 关于信息统一走站点元数据，版本号不再散落各处

## v0.2.0 · 2026-09-11 · 界面重构 + 题库扩充 + 每日一练

### 新增
- 每日一练：按当天日期散列选卷，同一天恒定、相邻日期不连号
- 题库扩充至 15 道完整题，五种题型各 3 道，含完整材料与参考答案
- 练习页自动载入今日一练，支持「换一题」从池中重抽
- 方格纸作答区，格宽随容器实测宽度自适应，折行严丝合缝
- 设置页状态总览：通道 / 模型 / Key 来源三栏一眼看清
- 阅读态材料：给定资料按「材料N」分则折叠，长材料不再刷屏

### 优化
- 导航改为左侧分组侧边栏（学习 / 内容 / 实战 / 复盘）
- 练习页左栏空态改为可搜索的选题面板，不再是大片留白

### 变更
- 界面风格换肤：新拟态 → 自然有机风，颜色收敛为语义 token

### 修复
- 修复从题库进入练习会丢失作答要求、分值、字数限制
- 修复桌面版被误判为「未配置 API」，导致批改按钮点不动

### 删除
- 移除 Arco Design 依赖，改自实现轻量提示框（CSS 405KB → 24KB）

## v0.1.0 · 2026-09-08 · 首个可用版本

### 新增
- 五位老师独立阅卷，可按 1~5 位自由组合
- 圆桌分歧复核与加权合议，分歧超阈值才触发复核
- 逐句批注：按老师颜色在作答原文上标出问题句
- 双通道：后端转发与浏览器直连自动降级
- 练习记录本地归档，可跨浏览器复盘
- 单文件 HTML 与 Windows 桌面版两种分发形态
- 老师讲义库与官媒时评文章库
