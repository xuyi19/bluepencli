# 更新日志

蓝笔申论（BluePencil）的版本改动都记在这一份文件里，**新的版本写在最上面**。

> 网页端的「更新日志」页**直接读这份文件**，所以改完保存就等于改好了页面，
> 不需要再同步任何前端代码；打包脚本也从这里取版本号给产物命名。

## 写法约定（解析器依赖，改格式前先看这里）

- 版本标题固定一行：`## vX.Y.Z · YYYY-MM-DD · 一句话标题`
- 改动分节写：`### 新增` / `### 优化` / `### 变更` / `### 修复` / `### 测试` / `### 删除`，
  每条以 `- ` 开头
- 一条写不完就**折行续写**（缩进两格接着写），页面会把它接成一句。
  接缝的补空格规则：中文之间**不补**（不然句中会凭空多一个空格），
  接到英文单词或符号（`/`、数字）时才补 —— 所以放心折
- 给某条挂子项：**缩进两格再写 `- `**，父条目末尾用「：」收口
- 写附注（比如"某个旧包有风险"）用 `>` 引用，页面渲染成单独一行小字
- 行内 `代码` / **加粗** / *斜体* 都支持，会渲染成元素，不会把记号原样显示
- 脚本只依赖第一条：版本号取**最上面那一版**。`desktop/scripts/publish.mjs` 与
  `frontend/scripts/publish-single.mjs` 都按这条规则读，读不到会直接报错停下，不会静默出个错包
- ⚠️ **改完跑 `node .tools/test-changelog.mjs`**。它盯的是「条目有没有丢、
  句子有没有被切断、记号有没有漏出去」—— 页面**能打开**不等于**能读**，
  v0.12.1 就是栽在这个区别上。

---

## v0.22.0 · 2026-09-24 · 答题页布局重排；荧光 8 色；侧栏与老师页翻新

### 新增

- **荧光色板扩到 8 色**（新增 橙 / 青 / 红），整体透明度提到 0.45~0.6——
  早先 0.34~0.42 太淡，划了像没划
- **侧栏每个入口配线性图标**；首页作为入口不再挂「学习」组头；
  「实战」提到第二位（这是本工具的核心动作，不该埋在第三组）

### 优化

- **答题页布局重排**：材料面板从 3/5 加宽到 **5/7** 并加高——读材料才是这个页面的大头；
  题目要求压缩到 2/7（题干 / 作答要求各留必要行数，元信息改两列小字）
- **老师页翻新**：列表卡带老师色活跃边条与合议权重徽章，评分维度加编号，
  讲义原文阅读排版放宽行距
- **标注交互加固**：mouseup 挪到 window 级——拖出面板外才松手也能弹色板
  （挂组件根上时 mouseup 不冒泡回来，是"有时候会有问题"的真实来源之一）；
  跨段选择不再静默，给出「请在同一段内划选」的提示

### 测试

- `probe-ui-structure.mjs`：侧栏组序与图标、答题页栅格比、老师页徽章/色条、
  色板 8 块的结构断言 + 截图

## v0.21.0 · 2026-09-24 · 荧光标注：材料与作答都能划重点，结果页与老师批注叠加

### 新增

- **荧光标注**：材料区划选文字即弹色板，五种颜色（黄/绿/蓝/粉/紫）标关键词、关键句；
  作答区新增「标注态」，一键从编辑切入，同样可划——编辑态与标注态分离，
  textarea 的原生输入体验一点不动
- **结果页叠加显示**：你自己划的荧光（背景色）与老师的色标批注（下划线）压在同一段作答上
  也能一眼分清；图例里「我的标注」与各老师并列，各自带处数
- **荧光失效如实提示**：作答改过之后，找不到的荧光单独列出（「这段话被改掉了」），
  绝不静默丢弃——悄悄少两处，用户会以为自己没划上

### 优化

- 标记存储用「文本片段 + 出现次数」锚点而不是字符下标：材料在「按题裁剪 / 整卷」之间
  切换后仍能重新定位，与批注 quote 同一套思路
- 标记按题目维度存本机（localStorage）：同一道题下次打开，上次划的重点还在；
  不写进批改记录，不涉及任何后端字段
- 作答区标题栏显示「已标 N 处」；结果页 README 同步重写（对齐 v0.20.0 实际形态）

### 修复

- **荧光色板定位值漏定义**：色板会失去坐标、糊在材料面板左上角——划了字「看起来没反应」。
  起因是探针用程序化构造选区（绕过了真实鼠标路径）+ 按类名找色块（位置错了照样点得到），
  两层都测不出位置错误；已把**真鼠标拖拽 + 色板位置断言 + Vue 告警零容忍**补进探针

### 测试

- `test-highlight.mjs`：荧光锚点纯函数 21 项（切片拼回一字不差 / 重叠先到先得 /
  失效归 stale 不静默丢）；`probe-highlight-ui.mjs`：真 Chrome 走「选区→色板→落标记」全链路

## v0.20.0 · 2026-09-24 · 三种练习形态齐备；每次批改都可追溯、采分更准

### 新增

- **真题模式**：`/real` 独立入口，**按原卷给全材料**，专门练「在整卷里找资料」——
  与练习批改的「按题引用裁剪」互补：一个贴近真实考场，一个省成本、更聚焦。
  至此三种形态齐备（练习批改 / 真题 / 考场），共用同一份作答与批改逻辑
- **每次批改都可追溯**：记录里冻结「标准来源与版本 / 采分点数 / 提示词版本 / 模型 /
  每位老师的温度 / 是否深度模式 / 分数来源」，结果页直接写出一行「批改依据：…」——
  半年后回看也能说清这个分数是怎么来的
- **可信度新增「标准来源」信号**：人工精校＝好、机器预解析＝注意（扣分）、无标准＝差；
  **无标准一律封顶「基本可信」**——几位老师一致只说明模型想法接近，说明不了判得对
- **采分点支持同义表述与反向要点**：同义表述让「换个说法」也算命中（此前只做字面
  关键词匹配，同义改写会被判漏）；反向要点让「答反了」明确不给分，且判据**最先判**
- **导入题库包时逐条回显**：读取文件 → 格式与校验和 → 签名校验（带 keyId）→ 解密 →
  水印 → 写入题库。加密包的价值（谁签发的、水印是谁）在导入这一刻就能看见，
  「签名不通过」与「口令不对」分开讲
- **「我的题库」批次管理**：按导入批次列出套数 / 题数 / 导入时间 / 文件名 / 水印 /
  签发者 / 卷清单，支持**整批删除**（二次确认 + 如实报数）
- **批量导入**：一次可选或拖入多个 `.bpq`，回显按文件分组，一个失败不中断后面的
- **复盘卡补上「✓ 这次做得好的 N 处」**：取老师与合议明确写出的肯定（带「为什么好」）。
  两条硬边界：**绝不从「没被批评」反推「做得好」**，也不拿「得分率高的分项」充数
- 考场模式时长补 120 分钟档

### 优化

- **硬规则事实前置给每位老师**（此前只当合议旁证，单人阅卷时那层约束等于不存在）

### 修复

- **题库页「自建」计数把导入的私有真题也算进去了**（导入 13 套后凭空多出 58 道）
- **私有卷数用 `Math.max` 单调累加、删除批次后不回落** —— 现按卷 id 去重取并集
- `data/standards/index.js` 引 `manual.json` 缺 `with { type: 'json' }`：
  Vite 能过、**Node 直跑会崩**，会让整条作者工具链在加载期挂掉，而当时测试全绿

### 测试

- 新增 `test-imports-library.mjs`（导入与我的题库，29 项）、
  `probe-import-ui.mjs`（造合法明文包真实拖入浏览器，22 项）
- 一键回归扩到 **21 套**，全绿
- **A1（辩论只发争议点）实测后判定不做**：量出收益上限只有 17.1%（材料早已按题裁剪），
  且该阶段需分歧 > 15% 才触发、历史从未触发。实测的价值是拦住一次接近 0 收益的改动

### 变更

- `docs/` 同步：使用说明书与「项目现状与规划」的「两种练习模式」改为「三种形态」，
  后续开发规划标记进度，两份方案对账文档同步

## v0.19.1 · 2026-09-24 · 考场模式独立成「实战」下的入口，与练习批改并存

### 变更

- **考场模式从页内切换改为独立入口**：它原先藏在练习批改页的一个「训练 / 考场」
  切换按钮里 —— 既不好找，而且考场中顺手就能切回训练去看参考答案。
  现在「实战」分组下并列两个入口：**练习批改**（随时批改、随时看参考答案）
  与**考场模式**（落笔计时、交卷后批改），首页也加了对应的入口卡
- 模式改由**路由**判定（`/practice` 与 `/exam` 共用同一组件）：一份作答与批改
  逻辑、两种形态，不会出现两份实现漂移；换页会自动重置倒计时与交卷确认，
  不会带着上一轮的秒数

### 测试

- 新增入口护栏 14 项：路由必须共用组件、侧边栏两个入口并存、模式必须由路由
  判定（禁止退回页内 chip 状态）、首页入口卡与网格布局

## v0.19.0 · 2026-09-24 · 考场模式上线，河北省考真题入库，题库可按体系筛选

### 新增

- **考场模式**：练习页新增「训练 / 考场」双模式切换（选择会被记住）。考场模式下
  落笔即计时（首次输入触发，贴真实考场「发卷后开始」），按题型给默认时长
  （大作文 60 分钟 / 贯彻执行 40 / 其余 30，可调），最后 5 分钟倒计时变红，
  交卷两段确认防误触，时间到自动交卷；交卷时定格作答用时，结果页显示
  「考试作答 N 分钟」。训练模式代码路径零改动，两种模式互不干扰
- **河北省考真题入库**：25 卷里 20 卷成功结构化（16 卷公开 2008–2021、
  4 卷私有 2022 起），题库从 24 套 119 题扩充到 **53 套 239 题**。
  提取器适配河北卷版式（`【材料一】`、`【问题N】`、三种答案锚点写法、
  页码与页眉噪声、「不少于 N 字」字数），并把全部国考卷重提做内容级对比
  确认**零回归**；5 卷暂缓（2004–2007 老版式/扫描件、2021 县级卷无文本层待 OCR）
- **题库页按体系筛选**：来源 → 体系（国考 / 省考-河北…）→ 题型 三维联动，
  体系那一排动态枚举（只有存在多种体系时才出现）；真题卡片标签改按体系生成
  （此前河北卷会被错标成「2019 年国考 · 县级卷」）
- **使用说明书**（`docs/项目文档/使用说明书.md`）：面向使用者的完整说明 ——
  三种打开方式、批改流程、五位阅卷老师的圆桌机制、两种练习模式、
  报告怎么读、复盘与成长记录、数据与隐私、常见问题

### 优化

- **作者侧工具链**（不在分发产物里）：管理员端整合为 11 个工具入口
  （题库/分发/文档/标准与回归），修掉「点了没反应」（Tauri 2 权限声明缺失）
  与「启动失败 os error 3」（相对路径按父进程目录解析），全程无控制台窗口；
  采分点校准工作台新增 `--draft` 材料预解析草稿（纯本地启发式，不调模型）
- 省考卷与国考卷**同口径按时间分层**：≤2021 公开、≥2022 私有；
  发布前的私有资产检查改为从卷清单动态计算公开卷数（不再手写死数字）
- 题库汇编新增「省考-河北」体系目录（现为 2 个体系 40 套 181 题）
- `docs/` 目录重组为 `题库/`（汇编 + 文章库 + 数据说明）、`practice/`、
  `项目文档/`，并加导航 README 说明每块是什么、谁生成、能不能删

### 修复

- **统计页成本与 token 恒为 0**：`/api/v1/stats` 的聚合取的是任务表里
  从不写入的两列，改为从调用日志表聚合（日志里的数据一直是对的）

### 测试

- 新增一键回归 `node .tools/test-all.mjs`：自动收集全部前端测试套件 + pytest，
  逐套输出耗时与统计摘要，发版前一条命令跑完
- 新增体系识别护栏 15 项（31 省识别、卷别统一排序、真实公开卷全量过识别）

## v0.18.0 · 2026-09-23 · 修复合议分数越界（得分率 330% 的根因），汇编改为目录树，管理员端出 GUI

### 修复

- **最终分越界钳制**：三人圆桌模式下合议阶段的 LLM 曾在一道 20 分题上输出
  66 分且被直接采信写进记录，统计页因此出现平均得分率 242%、单条 330% 的荒唐数字
  - prompt 层：合议系统的输出要求与 JSON 结构补上分数红线（finalScore 与
    各分项分必须落在 [0, 满分]，满分以题干「该题满分」为准）
  - 代码层：不信任 LLM 是最后一道闸的原则，单人/双人/圆桌三条路径的最终分
    统一在收尾出口钳到 [0, 满分]，分项维度分同口径钳制；满分一律以本地
    构建的题面为权威，合议返回的满分只作兜底不允许改写
  - 历史脏数据在读档入口归一化：旧记录（如那条 66/20）一被读取就钳回满分内，
    统计页无需重练立即恢复正常
  - 新增护栏测试 16 项（越界/负分/NaN/满分缺失回退/维度钳制 + 源码直读
    防回归：钳制必须存在且发生在可信度评估之前）

### 变更

- **题库汇编重构为目录树**：不再把 119 题平铺进两个大 md，改为
  「考试体系 → 年份 → 卷别」三级目录，每套卷独立 `材料.md` + `参考答案.md`，
  根 README 做总索引带链接；为后续接入省考各省份试卷预留体系层级。
  公开汇编 `docs/题库汇编/`（24 套 49 个文档）与私有汇编 `私有题库/汇编/`
  （9 套 19 个文档）已按新结构重新生成
  - 护栏测试同步改为按目录树断言（卷目录数、成对文档、标题对账、
    119 题段落对账），采分点泄漏检查覆盖全部导出文档，11 项全过

### 新增

- **题库管理员端 GUI**（`admin/`，独立 Tauri 2 小应用，作者专用）：盘点、
  打包（口令输入框）、验包、生成汇编四个功能 + 流式输出控制台，
  免记命令行；打包后为独立 exe，与分发的桌面版物理隔离，永不进用户安装包
  - 口令只经环境变量传给子进程，不进命令行参数（进程列表不可见），
    UI 侧不回显不落日志
  - 仓库根自动向上探测（找 .tools/admin-bank.mjs），找不到可手动指定并
    保存到 exe 同级 admin-config.json

---

## v0.17.0 · 2026-09-22 · 材料按题裁剪默认启用，题库汇编与管理员端上线

### 新增

- **材料按题裁剪，默认启用**：真题的 material 存的是整卷材料，而小题往往只问
  「给定资料4」——过去练习和批改都带着整卷五六则一起跑（实测材料占单次
  prompt 的 65%）。现在载入题目时自动只保留题干引用的那几则，
  材料 7,009 → 1,485 字（省 79%），单次批改输入约省一半
  - 三条硬规矩不变：题干解析不出资料号不裁、大作文不裁、引用的号在材料里
    找不到不裁 —— 宁可整卷提交，也不让老师看不到依据
  - 材料区显示「本题用给定资料N · 已省去其余 X 则」，一键「查看整卷材料」切回
  - 题库页每题补「材料预览」折叠，与练习页同一套裁剪口径
- **题库汇编导出**（`node .tools/export-bank-docs.mjs`）：公开卷
  （2010–2021 · 24 套 119 题）汇成《材料汇编》《参考答案汇编》两个文档加索引，
  按年份与考试分类，落在 `docs/题库汇编/`
  - `--private` 可把 2022–2024 私有卷汇到 `私有题库/汇编/`（gitignored，仅本机看）
  - **采分点与评分细则永不导出**（写出采分点＝泄题），护栏 `test-bank-docs.mjs`
    从 standards 提取指纹逐条断言零泄漏
- **题库管理员端**（`node .tools/admin-bank.mjs`）：盘点（list）/ 打包（pack，
  export_bpq 到 seal-bpq 一条龙，无口令拒跑）/ 验包（verify）/ 汇编（docs）
  四个子命令，包住既有加密工具，不重造轮子

### 优化

- 批改提交与界面展示使用同一份材料（裁后）——老师看到的就是考生读的，
  不会出现「批注引用了被裁掉的材料」这类错位

### 测试

- 新增 `probe-material-trim-ui.mjs`（11 项：题库页预览 + 练习页裁剪条 +
  整卷切换往返）、`test-bank-docs.mjs`（10 项：结构对账 + 泄漏检查 +
  公开/私有边界）
- materialTrim 头注释更新为「已接入」状态，防止后人再按旧注释误判

---

## v0.16.0 · 2026-09-21 · 采分点核对进记录页，能力画像改成固定六维

### 新增

- 记录页补「采分点核对」：回看旧记录时能直接看到**分扣在哪几个点上**，
  不用再翻几十条批注
  - 抽成共用组件 `KeyPointCheck.vue`，练习页与记录页走同一份渲染，
    免得两处各写一遍后慢慢漂移
  - 老记录没有归并结果时，从各老师的批注现场再归并一次 —— 直接拼数组会让
    「命中 X / Y 项」变成假数字（同一个点被数 N 遍）
- 能力画像：统计页的雷达图改成**固定六维** —— 要点覆盖 / 材料运用 / 结构条理 /
  审题应题 / 语言表达 / 规范体例
  - 每个维度都标了**算法口径**与参与篇数，没有口径的数字就是来历不明的断言
  - 没评过的维度显示「未评」，**不画进图里**（画到外圈等于说"这项你很强"）
  - 不满 3 篇不画形状，改提示「再练 N 篇」 —— 一两次的分数画出来的多边形
    只会误导复习方向，那比没有图更糟

### 优化

- 统计页的雷达图不再直接聚合老师给的 `dimensions`
  - 那是**模型自由文本**：「要点覆盖」和「覆盖度」会被当成两个不同的轴，
    轴的数量和内容随记录漂移，同一个人的画像隔一天就变了样
  - 现在统一走 `data/error-taxonomy.js` 的错误类型表，口径定死
- `prompt-size.mjs` 把「给定资料」单独拆出来显示，并给出裁剪收益的测算
  - 修了一个会让结论**完全对不上**的 bug：真题在池子里只是摘要、材料恒为 0，
    必须先 `resolveQuestion` 载入正文才量得到（否则会得出"材料 0 字符"）
  - 实测：一道只问「给定资料4」的真题小题，材料 7,009 字符里**只有 1,485 相关**，
    单次 prompt 能从 10,576 压到 5,052（省 52%）

### 测试

- `test-profile.mjs`（20 项）：盯的全是「看起来正常、其实是假信号」那类错 ——
  样本不足却出分、没数据的维度被画成 0 或 100、没批注的篇把画像整体推高
- `test-material-trim.mjs`（26 项）：材料裁剪的边界，重点是「该不该裁」
  - 大作文不裁、解析不出资料号不裁、引用的号在材料里找不到不裁
- `probe-profile-ui.mjs`（13 项）：真实浏览器里注入 3 条记录，验两处新 UI 真的渲染
  （这两个区块都是条件渲染，编译通过不代表用户看得到）

> `utils/grading/materialTrim.js` 已写好并配了单测，但**没有接入批改流程**。
> 取舍不在代码在判据：大作文本来就要通读全卷，裁错了会让老师看不到依据、分数变虚，
> 那比多花几分钱糟得多。要启用得先定「只在非大作文且解析成功时才裁」，
> 并且默认关闭、界面上能看见这次用了几则。

## v0.15.0 · 2026-09-21 · 桌面版补齐记账与记录归档：批完的稿子能落成 markdown 了

> 上一版把桌面端换成 Tauri 后，**前端有 10 个接口还是 404** —— 界面能开、能答题、能批改，
> 但批完的记录只存在浏览器 IndexedDB 里、成本报告永远没有数据。
> 而前端对这些端点是**静默降级**的（后端不可用就退回本地存储），所以界面上
> **看不出任何异常**。这一版把 Rust 侧补齐。

### 新增
- **记录归档**（`desktop/src-tauri/src/records.rs`）：批改完落盘成
  `docs/practice/<日期>-<标题>-<完整 id>.{md,json}`。位置在 **exe 同级目录** ——
  复制一份 exe 就等于带着自己的数据走（与旧 Python 桌面版行为一致）。
  五个端点：保存 / 列表 / 详情 / 取回 markdown 原文 / 删除
- **记账**（`desktop/src-tauri/src/db.rs`）：SQLite 落两张表 ——
  `grading_tasks`（任务汇总，前端上报）与 `llm_call_logs`（**逐次转发时后端自己记**，含 token）。
  成本数字只认后者：只有它知道每次调用真正花了多少
- **`paths.rs`**：统一数据落点。exe 同级不可写时（比如丢进 `C:\Program Files\`）
  退回 `%LOCALAPPDATA%\com.xuyi.bluepencil\`。判断方式是**真写一个探针文件**试一次 ——
  Windows 上光看属性位不可靠（目录带只读属性照样能写，ACL 拒绝时属性位又看不出来）
- 两个新护栏：`.tools/probe-tauri-data.mjs`（19 项，记账与归档端到端）、
  `.tools/test-md-parity.mjs`（2 项，md 渲染的两套实现逐字比对）

### 变更
- 表结构与 Python 侧（`backend/app/models/entities.py`）**逐字段一致** ——
  这样 `.tools/report-cost.py` 那套分析工具不用改就能读桌面版的库
- 流式转发里的 token 用量以前是丢掉的，现在拿去记账；并且加了**兜底**：
  上游不回 usage 时也记一条（token 记 0，但不能让这次调用从成本账上凭空消失）

### 修复
- **两份 md 渲染实现原本会漂移**：Windows 上 Python 的 `write_text` 会把 `\n` **静默转成**
  `\r\n`，而 Rust 的 `fs::write` 不会 —— 同一份记录，网站版与桌面版写出的文件不一样，
  且不报任何错。统一为 LF（与项目其他文本文件一致）。这个问题是逐字比对测试发现的

### 测试
- `probe-tauri-data` **19/19**：磁盘上真的生成了文件、md 渲染 9 处抽查、删除后文件真的消失、
  `score_rate` 算得对、时间戳与 Python 侧同一种格式
- `test-md-parity` **2/2**：全字段与极简两份样本，Python 与 Rust 的渲染结果**逐字相同**
- 产物 8.2 MB → **9.4 MB**（多了 rusqlite 自带的 SQLite）

---

## v0.14.1 · 2026-09-21 · 清掉 PyInstaller 时代的残留代码

> 桌面端换成 Tauri 2 之后，有一批只为旧桌面版存在的代码仍留在仓库里：不再被任何路径调用，
> 却会让人以为"这条路还在走"。这一版把它们删干净 —— **净减 1,692 行**，产物本身没有变化。

### 删除
- **PyInstaller 桌面版的整套入口**：`backend/desktop.py`（选端口 / 起服务 / 开浏览器）、
  `backend/build_desktop.py`（打包脚本）、`backend/release_utils.py`（打包共用的清理函数）
- **「关页即退」的看门狗**：`app/services/session_watch.py`、`app/api/v1/session.py`
  （hello / ping / bye / state 四个端点），以及随之失效的 `tests/test_session_watch.py`（13 项）
- **两个桌面版探针**：`.tools/probe-desktop-exit.mjs`、`.tools/test-desktop-reuse.py`

### 变更
- **Tauri 版不需要「关页即退」**：窗口就是进程，关窗口即退出，不存在"残留实例"这个状态
- **也不需要「复用同版本实例」**：Tauri 自带单实例插件，并自己选一个空闲端口
- `health` 的 `desktop` 字段**保留**：网站版必须能表达"我不是桌面版"，
  否则某个访客关掉标签页就会把公网服务杀掉 —— 只是现在已无人把它置真

### 修复
- 同步改掉指向已删文件的引用：`backend/app/main.py` 的路由注册、
  `.tools/test-route-eager.mjs` 里那条「桌面版开浏览器要带版本号」的断言（对象已不存在）、
  水印清单，以及 `README.md` / `PLAN.md` 的相关说明
- 本条之外的旧版本条目、以及 `docs/开发记录.md` 里的旧引用**故意保留** ——
  那是历史记录，改了就不是事实了

### 测试
- `pytest` 从 35 项变 **22 项**（少掉的 13 项正是会话看门狗的）
- 其余 11 个 Node 套件全绿；`test-route-eager` 从 6 项变 5 项，删掉的正是失效那一条

---

## v0.14.0 · 2026-09-21 · 桌面端换成 Tauri 2：45 MB 变 8 MB，产物收敛成两个

> 这一版把桌面端从 PyInstaller 换成了 **Tauri 2**，同时把产物砍到只剩两种：
> **网站版**与**桌面版**。单文件版与旧的 PyInstaller 桌面版正式退役。
> 对使用者来说最直观的变化：桌面版从「下载 24 MB 的 zip、解压、双击里面的 exe」
> 变成「下载一个 8 MB 的 exe，双击」。

### 新增
- **桌面端改用 Tauri 2**（`desktop/`）。前端仍是同一套 Vue 代码，**一行没改** ——
  Tauri 只负责把它装进一个原生窗口。产物是**单文件 exe（8 MB）**，
  内嵌了全部界面资源，免安装、免解压。
- **桌面版内置 Rust 网关**（`desktop/src-tauri/src/`）：桌面版不能像浏览器那样
  直接请求各家大模型接口（WebView 里是跨域，而厂商接口只面向服务端、不发 CORS 头），
  所以由 Rust 侧代发。它实现了 `health`、`llm/chat`、`llm/chat/stream`、
  `settings/test-llm`、`settings/llm-default` 五个端点，语义与 Python 版一致。
- **零配置**：桌面版读 exe 同级的 `config.json`（把 Key 预置进去发给别人，
  对方一个字都不用填），与 Python 版同一条规则。
- `.tools/probe-tauri-render.mjs`：桌面版渲染探针（26 项断言）。
  它盯的最要紧的一条是「页面加载的是**内嵌的那份**前端，而不是开发服务器地址」——
  见下面的修复第一条。

### 优化
- **桌面版体积 45 MB → 8 MB**。省下的是 Python 运行时：`libcrypto + libssl` 9.3 MB、
  `python313.dll + base_library` 7.3 MB、`pydantic_core` 5.0 MB —— 这几项占了旧包的一半以上，
  与业务代码毫无关系。Tauri 复用系统自带的 WebView2，不背浏览器内核。
- **仓库结构调整**，桌面端从 `frontend/src-tauri/` 独立成 `desktop/`：
  原先它把 `frontend/` 撑到 2.3 GB（那本该只有源码和 3 MB 的产物），现在回到 545 MB。
  构建工具（`@tauri-apps/cli`）跟着搬到 `desktop/`，前端只留运行时的 `@tauri-apps/api`。
- 产物目录 `release/` 改为**按产物通道分目录**：`桌面版/` 与旧通道各自带 `历史版本/`，
  回溯时不必在几十个文件里分辨"这是哪条通道的哪一版"。

### 变更
- **产物只保留两种**：网站版（`frontend/dist`，部署用）与桌面版（单文件 exe）。
- `release/私有题库/` 挪到项目根 `私有题库/`，并在忽略规则里**显式**声明。
  它原先只是顺带被 `release/` 整目录规则盖住的 —— 一条随时可能因为别的改动松掉的防线。

### 修复
- **桌面版点仓库地址没反应**。Tauri 默认会拦下 `<a target="_blank">`，
  而页面上完全看不出哪里错了（不报错、不白屏，就是没动静）。
  现在在入口统一接管：`http(s)` 与 `mailto` 链接一律交给系统浏览器打开，
  不用逐个链接去改（项目里这类链接散在侧边栏、首页、更新日志页、微信面板四处，
  **漏掉的那一处不会有任何报错**）。
- **桌面版「测试连接」永远失败**。Rust 网关最早漏了 `/settings/test-llm` 这个端点，
  前端拿到 404 只能显示一句"测试失败"，看起来像用户自己 Key 填错了。
  已补上，并在探针里加了断言专门盯这一类（端点漏实现 → 功能静默失效）。
- **桌面版双击没反应**。`tokio` 的监听器有个必须在异步运行时里创建的约束，
  放错位置会直接 panic；而 release 版没有控制台，连一行错误都看不到。
  同时把日志改成 release 也写文件（`%LOCALAPPDATA%\com.xuyi.bluepencil\logs\`），
  否则下次崩了同样抓瞎。

### 测试
- 新增桌面版探针 26 项：真渲染、加载的必须是内嵌前端、CORS 放行、
  设置页端点齐全、外部链接交给系统浏览器；`--llm` 模式下还会真发一次请求
  （用无效 Key 换上游 401）并用本地假模型跑通**流式**转发。

---

## v0.13.7 · 2026-09-18 · 给分数配一张「能信到什么程度」的证据卡

> 「AI 打的分能不能信」第一次有了可复核的答案，而不是一句自我声明。
> 唯一不变的是：**再可信也不给满分** —— 这个模块只能查「几个口径对不对得上」，
> 而这几个口径自己也可能一起错。

### 新增
- **评分可信度**（`utils/grading/credibility.js`）：一次批改算出五个**纯代码**信号，
  告诉你这个分数有多少依据：
  - `老师一致性` —— 各老师得分率的极差与离散度（`> 15` 个百分点的判据
    与圆桌辩论共用同一条线，不会出现"报告说有分歧、可信度说很一致"）
  - `与程序粗判的吻合度` —— 终评和「第①层按采分点标准算的覆盖率」差多少，
    这两个口径一个出自模型、一个出自代码，**互不相干却互相印证**
  - `阅卷完成度` —— 有没有老师的结果解析失败
  - `客观校验` —— 程序算出的扣分有没有撞到 20% 封顶线
  - `分歧复核` —— 触发了分歧、是不是真的走过一轮圆桌辩论
- `评分可信度`卡片（结果页与记录页）：每一项都**把算法口径原样写出来**
  （"程序按关键词粗判""按分值加权"），并列出「读这个分数前请注意」的提醒。
- 存档同步：可信度随记录**双写**进 IndexedDB 与 `docs/practice/*.{md,json}`，
  md 里排在标题正下方。半年后翻回一份旧记录，也能知道当时这个分数有多少依据。

### 修复
- **后端会把新字段悄悄吃掉**：`create_record` 走 `payload.model_dump()`，
  pydantic 对 schema 里没声明的字段**一律静默丢弃** —— 前端存了、docs 里没有，
  两边还不报错。已在 `PracticeRecordIn` 显式声明，并补了测试盯住这一点。

### 变更
- 单人批改时，一致性一律显示「**不适用**」而不是「分歧 0%」；
  下面还写清楚"这不是一致，是没得比"。**把无从判断显示成完美一致是最恶劣的假信号**。

### 测试
- 新增 `.tools/test-credibility.mjs`（34 项）：守的就是「不许说假话」——
  没依据必须闭嘴、掉链子必须降级、分数与等级必须自洽、
  判据不得与 `orchestrator` 漂移，以及把真实那次批改（终评 87.5% vs 程序粗判 85%，
  差 2.5 个百分点）钉在断言里。
- 后端 `test_records.py` 补 3 项：字段不被 `model_dump` 丢掉、md 渲染带口径、
  老记录（无此字段）照样能渲染。**新加的护栏必须验证过"错的时候会红"**。

---

## v0.13.6 · 2026-09-18 · 修正 Token 验收线的口径，并给出「为什么降不下来」

### 修复
- **成本报告的验收线用量纲错了层**。原先只有「单题 ≤ 2500 token」一条，
  而那 2500 是**按一次 LLM 调用**估出来的；三师圆桌一次批改天然是
  3 次 grade + 1 次合议 = 4 次调用，于是报告永远打「✗ 超出 6.2 倍」——
  看着像设计严重超标，其实是**拿整题去比单次预算**。
  现在拆成两条：① 单次调用 ≤ 2500（这条才是在控 prompt 体量）；
  ② 整题 ≤ 2500 × 调用次数（查有没有比线性更糟）。
  拆开之后能诚实回答：单次峰值 4,544，**确实超 1.8 倍**，且归因清楚（见下）。
- **自动选库会挑到全 0 的库**。本机同时存在好几个库（刚打包出来没跑过的空库、
  只用假 LLM 跑过链路的 `mock-model` 库、真正拿 Key 批过一次的那个），
  而挑选策略是「按写入时间」，于是经常挑到前两种 —— 报告打出
  「合计 0 tokens · ✓ 达标」，**看着像通过，其实什么都没测到**。
  现在优先挑**真的记账过调用**的库，并在列表里标出哪个有真实数据。
- **扫不到归档后的库**：候选库只扫 `release/` 的**一级子目录**，
  而产物归档之后真库跑进了 `release/历史版本/<版本>/data/`。
  **布局一变、扫描就跟不上** —— 这是本项目第 N 次栽在这上面，现在改为递归查找。

### 新增
- `.tools/prompt-size.mjs`：回答「单次调用为什么这么大、能不能优化」。
  报告（`report-cost.py`）说的是"实际花了多少"，这个说的是"钱花在哪"，两个对着看。
  实测构成：身份声明 3.7% + 老师方法论 31% + **通用铁律与输出契约 65%**
  + 采分点标准注入 30%（注入后口径）+ 题干资料 16%。
  **结论是构成里没有冗余项** —— 静态测算 3,579 tokens 对得上实测 prompt 3,312，
  拆解是准的，不是拍脑袋的百分比。

> 结论要说清楚：**想把数字压下去只能牺牲批改质量，不划算。**
> 那 2500 是当初估低了（没把"标准注入 + 输出契约"算进去），不是后来写坏了。
> 真要省钱有两条无损的路：① 用支持 prompt caching 的厂商
> （每位老师拿到的"通用铁律 + 输出契约 + 标准"完全相同，是理想缓存前缀）；
> ② 减少老师人数。当前单题成本 ¥0.0097，优化的**经济收益趋近于零**。

## v0.13.5 · 2026-09-18 · 修探针的「假绿」：它测的可能是别人的实例

### 修复
- **自动化探针可能测到别人的实例，还报"全部通过"**。发现过程很值得记：
  跑关页即退探针时报 2 项失败（`sessions` 变成 2、`bye` 之后只降到 1），
  一度以为是"关页即退"坏了。查下去才发现 8765 上残留着一个**上一版的桌面版**，
  探针扫端口时只判「是不是蓝笔申论、是不是桌面版」，于是**认了那个残留实例当自家人**：
  它本来就保着 1 个会话，所以 `sessions` 是 2；`bye` 清掉的只是探针自己的页面，
  所以降到 1 而不是 0，进程自然也不退。
  **更糟的是反过来**：残留实例行为凑巧正确时，探针会报"通过"，
  而它自己起的那个进程压根没起来 —— 这才是最危险的，**假绿比假红代价大得多**。
  现在探针**显式用独立端口起实例**，并在起进程前拍一张端口基线快照，
  只认"基线里没有的那个"；发现同族实例会先打印出来说明不参与断言。
- **`desktop.py` 支持 `--port N`**。这不是测试专用后门：默认端口上已有**同版本**实例时，
  「同版本复用」逻辑会直接退出并把浏览器指向那个旧实例 —— 对用户是对的，
  但让自动化没法保证"断言打在自己起的那个进程上"，所以需要能换端口起。
  顺带也给了用户一个显式指定端口的入口，默认行为完全不变。

### 测试
- 实例复用测试从 4 项扩到 6 项：新增「显式 `--port` 必须绕过同版本复用」
  与「`--port N` / `--port=N` 两种写法都认，非法值与缺参数退回默认」。
  后一条防的是**静默退回默认端口**——那种失败看起来完全正常（服务起在 8765 上了），最难发现。
- 关页即退探针加了两道环境护栏：起进程前打印已存在的同族实例、
  专用端口被占时直接报错停下而不是跑偏。

> 教训：「我这边是好的」和「真的测到了」之间，隔着**"测的是不是我起的那个"**。
> 这和 v0.13.2 修的「桌面版复用旧实例、把用户带去旧界面」是同一个病根，
> 只是这次发生在测试工具这一侧 —— 同一个坑，一个脚本有护栏、另一个没有，
> 缺的那一个就会把假绿端出来。

## v0.13.4 · 2026-09-18 · 第一次真实批改暴露的三个问题（采分点归并 / 价目 / Token）

### 修复
- **多老师批改时，采分点会被重复计数** —— 这是第一次真实批改（三师圆桌）跑出来才发现的：
  结果页显示"6 条采分点"，但那 6 条是三位老师各自的 keyPoints **直接拼接**出来的，
  权重合计 21 分而标准只有 20 分。更阴的是**条数碰巧等于标准点数**，
  光看数量根本发现不了，只有算权重合计才露馅。现在改成按采分点标准**逐点归并**：
  同一采分点合并为一条、命中状态取最好的那位老师、权重合计恒等于标准满分。
- **结果页的兜底路径犯了同一个错**：`final.keyPoints` 缺失时会退化成拼接，
  老记录（修复前归档的）照样显示假数字。现在兜底也走归并，历史记录一并修正。
- **老师全挂时标准点也没了**：归并函数里有个提前返回，导致"模型全部调用失败"
  这种最需要看到采分点的时刻，页面上一片空白。现在标准点照常列出、全部标为缺失。

### 变更
- **老师输出契约收紧了 keyPoints 的粒度**：要求"一条只写一个点"、有标准时必须回填
  pointId。真实数据里老师会把"组织保障+资金投入+标准体系"塞进同一条长句，
  而一条只能填一个分值，归并时必然错配 —— 这是上面那个 bug 的**根因**，不只是被它触发。
- **成本报告改按实际模型折价**。以前只有一个写死的 DeepSeek 价目，
  而真批改用的事实上是智谱 glm-4-flash —— 报告里的"¥0.0000"不是因为便宜，
  是因为任务表的 token 字段永远是 0，**那个数字压根没被填过**。
  现在：① 报告从逐次调用明细现算 token；② 按调用实际用的模型查价目表；
  ③ 未收录的模型会明确提示按兜底价估算。

### 测试
- 新增采分点归并的单测（34 项）与**真实数据回放**（15 项）。
  回放用的是第一次真批改的原始输出，并同时算出修复前/后的口径做对照 ——
  「修复前 21 分、修复后 20 分」这个对照关系，比断言一个孤立的数字更能说明问题。
- 回归锁死两条真实踩坑的边界：一句话里说两个采分点、模型全挂时的标准兜底。

> 实测结论（第一次真批改）：三师圆桌 15,598 token / ¥0.0097（glm-4-flash）／
> 耗时 116 秒／老师给 17.5 分，程序按标准粗判 85%，两者差 3 个百分点。
> 唯一没答到的点是「乡村微治理」——三位老师都漏了，这是一句可验证的实话。

## v0.13.3 · 2026-09-18 · 「再练一题」：按短板推下一题（V3）

### 新增
- **错题本页多了一张推荐卡**，回答"看完这些该干什么"：按你反复栽的问题，
  从题库里挑下一道题，点「练这道」直接带着题目跳到练习页。
- **判据分两档，而且分开说**：
  - **同题型对症** —— 某类问题**集中**在某个题型上（该题型栽了 ≥2 份、且这类问题
    主要就出在这个题型），推同题型。话术会点名"你在归纳概括上反复漏点"。
  - **跨题型通病** —— 同一类问题在 ≥2 个题型上都出现（比如「整句照抄」是习惯问题，
    换什么题都一样），推任意题型，并说明"不是某个题型的问题"。
  两档都不满足时**不给推荐**，如实说样本还不够 —— 随便挑一道让用户以为有依据，
  比空着更糟。
- **批改记录开始存题型**（`questionType`）。这是这件事的拦路石：题型原本只活在
  练习页的 UI 状态里，批改完就丢了，于是"在哪个题型上栽的"根本无从判断。
  老记录没有这个字段 → 照常进错题本（批注都在），但推不出下一题，
  UI 会说明是缺字段而不是"没练过"。

### 变更
- 不按难度递进：题库里 `difficulty` 是 1~5 的自评，同题型只有 3 道，
  按它排序等于随机 —— **没有依据的排序比不排序更糟**，用户会以为它有意义。

### 测试
- `.tools/test-next-question.mjs`（18 项）：盯的是**它什么时候该闭嘴** ——
  零记录 / 只有 1 份 / 只有缺题型的老记录，三种情形都必须不给推荐且说明原因；
  另验「问题只跨一个题型且池里没该题型时不拿别的题型凑数」。
- `.tools/probe-next-question.mjs`（13 项）：真浏览器注入构造记录，覆盖定向 / 泛化 /
  不给推荐三个分支，并用真鼠标事件点「练这道」验证跳转与真题懒加载。
  ⚠️ 这里踩过一次**假通过**：断言原本只截页面正文前 1200 字（全是导航栏），
  靠一个宽泛词（「给定资料」）在别处命中侥幸为绿，而题干其实压根没渲染。
  **断言要盯题干里才有的专有名词，取景窗口要够长。**

---

## v0.13.2 · 2026-09-17 · 路由改静态导入，根治「点导航没反应」

### 修复
- **根因钉死了**：既不是打包漏了资源，也不是修得不对 —— 是**那个页面根本收不到修复**。
  桌面版每次启动都开同一个地址，浏览器直接复用早就打开的标签页；实测那一个标签页是
  **v0.8.1** 留下的（它请求的分片名 `ChangelogView-Cd9uacJ9.js` 正是 v0.8.1 的产物），
  页面里跑的是 v0.8.1 的代码 —— 后面每一版的修复都到不了它手上。
  查了三轮才把"分片失效"和"标签页一直没换"这两件事串起来。
- **路由改为静态导入**，不再用懒加载分片。导航只渲染一个已经在内存里的组件，
  **不产生任何网络请求**，也就没有"取不到"这回事。
  原来的机制是：每个页面一个按内容 hash 命名的独立文件，升级即失效，
  老页面照着旧地址去取就是 404，vue-router 随即中止跳转 —— 用户看到的就是「点了没反应」：
  不白屏、不报红字，只有控制台里一行错。
- **入口加载后自检版本**：问一次 `/api/v1/health`，页面版本与服务端不一致就刷新一次
  （按服务端版本记账，同一个版本只刷一次，不会停不下来）。
  往后升级就不必指望用户手动刷新了。单文件版是 `file://` 打开的、没有后端，
  请求失败即静默跳过。
- **桌面版开浏览器时带上版本号**（`http://127.0.0.1:<端口>/?v=...`）：
  URL 不同，浏览器就必须重新加载文档，不会再把人带到旧页面上。

### 测试
- 删除 `probe-stale-chunk.mjs` —— 它验的是"分片失效后能否自愈"，而分片已经不存在了。
- 换成构建级护栏 `test-route-eager.mjs`：源码里不许出现 `() => import(`、
  产物里不许有 `*View-*.js` 分片、主包必须真的包含页面代码、
  入口必须有版本自检、桌面版必须带版本号。
  **懒加载是个太顺手的好习惯，不钉住迟早会被改回去。**

### 变更
- 首屏包由约 100 KB 增至约 1.8 MB（gzip 657 KB）：页面代码全部收进主包。
  桌面版与单文件版都在本地加载，这点体积换"点了一定能跳"是划算的。

---

## v0.13.1 · 2026-09-17 · 修「升级后点导航没反应」

### 修复
- 点侧边栏入口**没反应**这件事查到底了：不白屏、不报红字，控制台里只有一行
  `TypeError: Failed to fetch dynamically imported module`。
  v0.12.2 修的「入口太小」是另一条路，两条都能造成同一句抱怨。
- **成因**：每个页面是懒加载的**独立分片**，文件名按内容 hash 变化。升级后旧分片被删掉，
  而已经打开的页面手里还攥着旧 bundle，它去取那个早已不存在的地址 → 404 →
  路由跳转被中止，地址也被退回原处。
  桌面版最容易撞上：每次升级都落在**同一个端口**（关页即退，端口随即释放），
  「新进程 + 老标签页」这个组合几乎必然出现 —— 一天连发三版就更容易了。
- **修法**：出错时记下用户想去哪儿，整页重新加载一次；下次启动时
  **在 `createRouter` 之前**把地址改回那一条。必须是"之前"——
  `createWebHashHistory()` 是读 `location.hash` 决定初始路由的，这时候改，
  出来直接就是那一页。**只刷新不记目的地是不行的**：探针第一次跑出来
  `loads=2` 却仍停在首页，因为中止跳转时地址已经退回去了。
- 万一刷新后仍取不到（服务端本身坏了），用 `sessionStorage` 时间戳挡一次不无限刷新，
  并给一条看得见的提示（Ctrl+F5）。提示挂在 `documentElement` 上而不是 `#app` 里，
  免得被 Vue 接管。

### 测试
- 新增 `probe-stale-chunk.mjs`：自带静态服务器，按请求把 chunk 名**实时改写成旧名字**，
  再切阶段让旧名字 404 —— 用它**忠实重放「升级那一刻」**，断言点击后能自愈，
  并且回到用户本来要去的那一页。
- ⚠️ 别把阶段顺序搞反：先切阶段再点击，第一次请求就 200，探针会"绿得很好看"
  却根本没复现出 404。**直接渲染成功 ≠ 点得到**，v0.12.2 已经栽过一次。

---

## v0.13.0 · 2026-09-17 · 错题本列表页（V3）

### 新增
- **错题本页**（侧边栏「复盘」组）。它回答的是复盘卡回答不了的那个问题：
  复盘卡看的是**这一次**，错题本看的是**一段时间** —— 同一类毛病在多份记录里反复出现，
  才算真短板，所以排序按「**栽在几份记录里**」而不是「被提了几次」。
- 页面分两层：**反复出现的问题**排在前面（附动笔前自检，以及"第 N 次"直接跳回那次批改）；
  只出现过一次的收进折叠区 —— 给数量，但不喧宾夺主。
- 没有记录时给空态引导（去练习一题）；只批改过 1 份时**不假装有结论**，
  明说"同一类问题要在至少两份记录里都出现才算反复栽的坑"。
- 这一页的价值全在**口径**上：`漏点` / `要点不全` / `采分词缺失` 三种写法能聚成同一类，
  靠的是 v0.12.0 定下的错误类型口径。没有那一步，同一个毛病会被算成三类，
  错题本会**假性稀疏** —— 看起来"没什么反复问题"，其实全是重复的。

### 测试
- 新增 `probe-weakness.mjs`：往 IndexedDB 注入 3 条构造记录（两条含同类问题、
  一条只含单发问题），验证页面真的按「栽在几份」聚合、单发问题收进折叠区。
  为什么注入而不是手点：走 UI 批改一遍要真 Key、要几分钟，
  而注入能把「1 份时显示空态 / 2 份同类才冒出反复问题」这两个分支都覆盖。

---

## v0.12.2 · 2026-09-17 · 修「点侧边栏日志没反应」

### 修复
- **侧边栏的「日志」入口点不动**。它和 GitHub / Gitee 挤在同一行三等分，
  每个只有 61×28px、间隙只有 6px —— 鼠标或触控板稍偏一点就落进缝里，
  症状是"点了没反应"，很容易被当成"页面坏了"。日志是会主动去翻的入口，
  现在提升成导航里正常尺寸的条目（199×40px）；底部只留 GitHub / Gitee 两个胶囊，
  它们也因此变宽了。
- 排查时先排除了产物问题：v0.12.1 的 `dist` 与桌面版内嵌 `web` **逐文件一致**
  （assets 都是 54 个、`index.html` 是同一份），没有缺失 chunk；
  在真实浏览器里点它**也能跳转**，页面渲染出 14 版 118 条。
  所以问题不在"发错了包"，而在**点击目标太小**这个纯可用性缺陷上。

### 测试
- 新增 `probe-nav-click.mjs`：用真实 Chrome 的**鼠标事件**逐个点侧边栏入口
  （走坐标命中测试，而不是 `element.click()`），看 hash 变没变、目标页渲染没渲染，
  并量出每个链接的**实际尺寸**，小于 40×32 直接报出来。
  为什么不用 `element.click()`：它把事件直接派给元素、**绕过遮挡**，
  被盖住或被挤出视口外的入口照样"成功"，测了等于没测。
- 这次漏检的根因值得记一笔：`probe-changelog.mjs` 是**直接访问** `#/changelog` 的，
  把"用户从侧边栏点进来"这条真实路径整个跳过了 ——
  **能渲染不等于点得到**。新探针补的正是这一段。

---

## v0.12.1 · 2026-09-17 · 修「更新日志页读不了」

### 修复
- **更新日志页把 Markdown 原样显示，长条目还被截断**（v0.8.1 起逐渐变严重）。
  页面用的解析器只认上面那段最小约定（版本行 / 分组 / **单行**条目），
  而这份文档从 v0.8.1 起越写越按 GitHub 风格来，于是三件事同时发生：
  - 104 条里 **63 条**把加粗记号、行内代码记号当普通文字吐出来；
  - **折行的后半句整段消失**（v0.12.0 第一条断在「分类混乱 /」，后面全没了）；
  - **缩进子条目一条没渲染**（v0.11.0 里 issue.mjs 的三个子项看不见）。
  最难受的是页面**没崩**：版本数、顺序、「每版都有条目」全部对得上，
  于是所有「能打开吗」的检查都放行了 —— 问题是它读不了。
- 解析器拆成 `frontend/src/data/changelog-parse.js`（纯函数，不依赖 Vite 的 `?raw`），
  让单测 import 的是**页面在跑的那份代码**。留在 `changelog.js` 里的话，
  测试只能把正则复制一份过去 —— 而复制的那份永远是对的，页面照旧能坏。
- 现在支持：**折行续写**（中文之间不补空格，接英文才补）、**缩进子条目**、
  `>` **引用附注**（单独一行小字，不糊进正文）、行内 `代码` / **加粗** / *斜体*。
  渲染走**片段化**而不是 `v-html`：文档是自己写的，但「把 Markdown 当 HTML 解析」
  这个口子不该开。
- 行内解析踩了一个顺序坑：**不能**先把代码段切下来再各自找加粗 ——
  加粗跨过代码段时（像「发放工具 issue.mjs」这种写法），切开后两边各剩半个记号；
  反过来整行匹配又会被代码里的 `*`（如 `/wechat/*.jpg`）当边界而失配。
  改成用占位符把代码**挖走**（不是切开）再整体匹配，最后还原。
- `probe-site-links.mjs` 修掉一条**失效了六个版本**的断言：它还在首页找「更新日志」胶囊，
  可那个入口 v0.6.0 就挪到侧边栏底部了（且侧边栏上的字是「日志」），
  于是这条断言从那时起一直在报假失败 —— 这条探针要起 dev server 才跑，
  没人跑就没人发现。改为窄屏下打开抽屉、断言入口真的够得着。

### 新增
- `### 测试` 这一节在页面上有了自己的颜色。原来它落到默认灰样式，
  跟「认不出来的分组」长得一样。

### 测试
- 新增 `test-changelog.mjs`（23 项）。看的不是「渲染了几个版本」，而是**内容完不完整**：
  条目有没有丢、句子有没有被切在半路、记号有没有漏出去、
  **续行拼接是否真的生效**（直接钉住「整句照抄」「私钥 gitignore」这些折行处的词）。
- `probe-site-links.mjs` 增加四条**可读性**断言：记号渲染成了元素、折行后半句在、子条目在。

---

## v0.12.0 · 2026-09-17 · 错误类型统一口径 + 复盘卡（V3 起步）

### 新增
- **错误类型表**（`data/error-taxonomy.js`）：12 个固定分类，如「要点遗漏 / 拆点 / 分类混乱 /
  整句照抄 / 概括失真 / 空话套话 / 形式不应题 / 结构失当 / 字数失当 / 格式体例 / 表述不规范」。
  每类带一句**能照着做的**「动笔前自检」，复盘卡的检查清单直接用它生成。
- **自由文本归一化**：老数据里模型写的 `type` 是自由文本（「采分词缺失」「漏点」「要点不全」
  其实是同一件事）。归一化函数把它们收进同一格，认不出来的一律归「其他」——
  **宁可漏归一类也不硬塞**，错塞会让复习重点指错方向，比不分类更误导。
- **prompt 改用固定口径**：新批改的 `type` 从表里挑 id，一句话犯两种毛病时写成
  `copy-raw,over-generalize`。这样跨记录统计才立得住。
- **复盘卡**（练习页结果区 + 历史批改详情页）：把一份批改压成「下次动笔前先改这 N 处」，
  每处给出原文引用、改法、扣分合计，最后附一张动笔前自检清单。
- **短板统计**（`buildWeaknessProfile`，数据层就绪）：跨记录按类型聚合，
  排序按「栽在几份记录里」而不是「被说了几句」——同一份答案里同一位老师反复说同一件事，
  不该比连续三次练习都掉进同一个坑更值得复习。

### 优化
- 复盘卡排序按**共识优先**：几位老师**各自独立**提到同一类问题 = 硬伤；
  只有一位提到可能只是他的侧重点。共识相同时看扣分，最后看出现次数。
  单人模式下「共识」这件事不成立，界面会**如实改口径**（只说「1 位老师提到」），
  不拿「老师都提到了」唬人。
- 排序不需要再调一次模型：意见已经在批改结果里，再调一次只是换个说法，
  还会引入「两处说法不一致」。这里是纯代码聚合，可复算。
- 侧边栏分组名与条目名不再同名（分组「复盘」下的条目「复盘」→「历史批改」），
  原来侧边栏会出现两行一模一样的字，看着像重复项。

### 修复
- `skills.js` 的 prompt 里混进了反引号，把外层模板字符串截断，构建直接失败
  （`vite-dynamic-import-vars` 报"无法解析"）。prompt 是一整个模板字面量，
  里面出现反引号必须换掉。
- 归一化规则顺序钉住两处易混：`展开不足` 里含 `不足`，必须排在「字数失当」之前，
  否则「分论点展开不足」会被判成字数问题；「形式不应题」与「结构」类关键词可能同时出现
  （如"形式不应题、结构也松散"），应题类必须排在结构类之前才不会被抢走主类。

### 测试
- 新增 `test-review-card.mjs`（37 项）：归一化 37 例（含 4 组反例专测）、
  复合 id 写法、共识排序、单人模式口径、`其他` 不上榜、去重与条数上限、跨记录短板统计。
- `e2e-grade.mjs` 增加复盘卡断言：用假 LLM 跑完整批改链路，
  验证三位老师的自由文本批注（「采分词缺失」「空泛表态」）真的聚成了两类并命中共识标记。

---

## v0.11.0 · 2026-09-17 · 拖拽导入题库包 + 发放批次台账（V2 收尾完成）

### 新增
- **题库包拖拽导入**：把 `.bpq` 文件拖进窗口任意位置就能导入，不用再去点按钮找文件。
  拖拽期间整页给一层提示，松手即入。加密包照旧会弹口令框——
  拖拽和"选文件"走的是**同一段导入逻辑**，不会只有一条路支持加密包。
  监听挂在 `window` 上而不是某个框上：用户拖文件时不会瞄准某个小元素，
  落点在哪都得接住；落点限制靠扩展名过滤（`.bpq` / `.json`）而不是靠绑小元素。
- **发放工具 `issue.mjs`**（作者专用）：
  - **口令批次**：同一批人共用一个口令，换一批人必须换口令。新批次的口令一律
    **随机生成**（避开 0/O、1/I 这类口头传递会听错的字符），
    并且显式想复用别的批次的口令时会**直接拒绝**（`--allow-reuse` 才能放行）——
    "图省事一直用同一个口令"的代价是一人泄露、全批人的包都成了公开资源。
  - **发放台账**（`release/发放台账.{json,md}`，不进版本库）：记下发给谁、哪一批口令、
    哪个文件，以及**文件当时的 sha256**。哈希是这里的关键——事后对方手上那份包流转出去，
    能靠哈希确认"流转的就是我发的那份"，再配合包内的使用者水印追到人。
    `--verify-ledger` 会重算哈希核对，包被改过就报不一致。
  - **按人改水印**：一份明文包 + N 个收件人 → N 份包，**水印各不相同**，
    且每次都重新加密（换 IV），所以同一个包发给两个人，密文也不一样。
- **`seal-core.mjs`**：把"明文包 → 加密包"抽成唯一实现，
  `seal-bpq.mjs`（手工封一个）与 `issue.mjs`（按批次批量发）共用。
  各写一份的话，签名覆盖字段一旦有细微差别，症状是"手工封的能开、批量发的开不了"，极难定位。
- 新增两组真浏览器 / 真数据测试：
  `probe-pack-drop.mjs`（真 Chrome 12 项：提示层显隐 / **划过子元素不闪** /
  纯文本拖拽不误触发 / 松手 .bpq 真的走完导入 / 不支持的类型无副作用）；
  `test-issue.mjs`（24 项：换批换口令的约束 / 同批次同口令但水印各异 /
  台账哈希对得上 / 包被改后核对报错）。

### 修复
- `issue.mjs` 台账时间改用**本地时间**：原来走 `toISOString()` 是 UTC，
  本机 +8，凌晨发的包（本地 07:00 = UTC 前一天 23:00）会被记成前一天。

### 变更
- `.gitignore` 增加 `.tools/exams/batches.json`（存着口令原文，进库等于公开私有题库包）。

---

## v0.10.0 · 2026-09-17 · 题库包加密 + 签名（V2 收尾）

### 新增
- **题库包加密（.bpq v2）**：AES-256-GCM，密钥由分发口令经 PBKDF2-SHA256（20 万次）派生。
  正文（材料与参考答案）加密后放进 `crypto` 段，**包本身外流也读不出内容**。
  口令由作者**另外告知**——和包走两条路，一起发等于没加密。
- **ECDSA P-256 签名**：证明"这个包确实是作者签发的"。
  **GCM 的完整性不等于身份认证**：知道口令的人都能造出合法密文，签名才能把作者身份钉死。
  签名覆盖水印字段，所以改水印也会验不过——「泄露可溯源」这句话到这才有依据。
  验签排在解密**之前**：伪造的包直接拒掉，不必白跑一次 PBKDF2。
- **作者侧两个工具**：
  `bpq-keygen.mjs` 生成密钥对（公钥自动写进 `frontend/src/bpq/pubkey.js`，私钥 gitignore）；
  `seal-bpq.mjs` 把明文包封装成加密包，自带"回读解密"自检与 `--check` 复核模式。
- **导入时弹口令框**：题库页导入加密包 → 遮字输入框 → 最多试 3 次。
  `ToastHost` 因此多了 `toast.askPassword`（仍是自实现，不引 UI 库）。
- 新增两组测试：
  `test-bpq-crypto.mjs`（20 项：口令错 / 密文被改 / **水印被改** / 换成他人公钥 /
  他人私钥冒充 / 老包回归 / 作者命令行通路）；
  `probe-bpq-browser.mjs`（真浏览器 7 项，验**跨运行时**互操作：Node 加密 → 浏览器验签解密）。

### 变更
- **公钥改成数组**（`BPQ_PUBLIC_KEYS`）：轮换密钥时**追加**而不是替换——
  删掉哪一把，之前发出去的包就永久验不过签了（那些包用户手上还在用）。
- `.bpq` 新增 v2 格式，**v1 明文包继续支持**（已发出去的包不能失效）。
  导入时按 `magic` 自动分辨（`BPQ00001` / `BPQ00002`）。
- `verify-bpq.mjs` 支持加密包（`--passphrase`），校验内容按格式分流。

### 优化
- 加密与签名统一走 **WebCrypto**（Node 与浏览器是同一套 API）：后端**零新增依赖**，
  也绕开了"Python 的 ECDSA 默认输出 DER、而 WebCrypto 要 raw(r‖s)"这个必踩的坑
  （踩了的话症状是"验签永远不通过"，且看不出原因）。
- 明文包与加密包文件名区分（`-加密` 后缀）：`release/私有题库/` 里哪个能外发一眼可辨。

### 修复
- `openSealedPack` 传入**空公钥列表**时会静默回退到内置公钥 ——
  于是"程序里压根没有验签公钥"被误报成"签名不通过"，把用户引去怀疑包本身。
  现在明确区分「没传」（用内置）和「显式传空」（就是没有可用公钥）。

---

## v0.9.0 · 2026-09-17 · 桌面版：关掉浏览器页面就自动退出

### 新增
- **桌面版「关页即退」**：浏览器页面全部关掉后，程序自己结束进程（黑窗口随之消失），
  不用再去手动关窗口。此前只认「关黑窗口」，用户关掉页面后普遍以为程序已经退了，
  可进程还在后台占着默认端口 —— 下次双击就命中残留实例，看着像"启动了"，
  其实什么也没重启（升级后打开旧界面的问题，根子就在这）。
  **判定三条同时成立才退**：曾经有过页面会话 + 当前没有活跃会话 + 这个"空"状态
  已持续超过 6 秒。第三条是给**刷新**留的窗口：刷新等于「先 bye 后 hello」，
  新页面在 6 秒内注册回来就把倒计时取消掉。
  **心跳不参与判定**，只用来回收僵尸会话 —— 后台标签页的定时器会被浏览器节流，
  拿它当"页面还活着"的证据会把正常开着的页面误杀。
- **会话端点**：`/api/v1/session/hello`（注册 / 复位倒计时）、`/session/ping`（心跳）、
  `/session/bye`（注销）、`/session/state`（查看现状）。
  `bye` 走 POST 是有意的：页面卸载阶段只有 `navigator.sendBeacon` 的 POST 发得出去，
  普通 `fetch` 会被浏览器掐掉。
- `health` 新增 **`desktop` 字段**：前端据此决定挂不挂这套逻辑。
  网站版 / 单文件版一律不挂 —— 否则某个访客关掉标签页就会把别人的服务杀掉。
- `backend/tests/test_session_watch.py`：**13 项用例**，正反两个方向都钉住 ——
  刷新不误杀、多标签只关一个不误杀、从没开过页面不误杀、崩溃后僵尸会话会回收、
  退出只触发一次。全部直接喂时间戳复算，不真的等 6 秒。

### 变更
- `desktop.py` 改用显式的 `uvicorn.Server`（原先用 `uvicorn.run` 的语法糖），
  这样才能拿到 server 句柄做**优雅退出**：关页后照常走完 lifespan 收尾
  （关掉 httpx 客户端、dispose 数据库连接池）再退，而不是把进程直接砍掉。

### 优化
- 黑窗口提示改为「关掉浏览器页面就会自动退出，也可以直接关掉本窗口」。

---

## v0.8.1 · 2026-09-14 · 产物自证版本 + 修「升级后还是旧界面」

### 修复
- ⚠️ **最要命的一条：升级后双击新 exe，打开的却是旧界面**。`desktop.py` 原本只判断
  「默认端口上跑的是不是 BluePencil」就复用实例、**不比对版本** —— 用户升级后旧窗口
  往往还开着，于是新 exe 命中旧实例、把浏览器指过去、自己退出。现象就是「解压出来
  是旧版的」，而新版其实已经装好了，且新进程的日志一切正常（所以极难自查）。
  现在**只复用版本相同的实例**；版本不同就另起端口，并打印
  「端口 8765 上运行着旧版本 v0.7.0，当前是 v0.8.1，新版本已改用 …:8766」。
- **桌面版升级后打开的仍是旧界面（缓存那半边）**：静态资源由 FastAPI `StaticFiles` 直接托管，
  不带任何 `Cache-Control`，浏览器走启发式缓存把入口 HTML 留住了。
  桌面版每次升级都跑在同一个地址（`127.0.0.1:8765`），于是"解压了新包、看到旧界面"，
  看着像发错包，其实是缓存。现在入口 HTML 一律 `no-cache, must-revalidate`
  （靠 ETag 拿 304，不额外费流量），带内容哈希的 js/css/图片长缓存。

### 新增
- **产物自证版本**：构建时把 `app-version` / `app-release-date` / `app-build-time`
  写进 `index.html` 的 meta（`vite.config.js` 的 `versionStamp` 插件，版本取 CHANGELOG 首版）；
  桌面版另附一个 **`版本信息.txt`**，并在「使用说明.txt」标题带上版本号。
- **`release/历史版本/`**：历史产物挪进子目录，`release/` 根只留最新一版。
  此前多个版本的包平铺在一起、文件名只差一处版本号，随手解压一个就是旧版。

### 优化
- `.tools/probe-wechat.mjs` 支持 `BP_BASE` 环境变量，同一份断言既能验源码
  （dev server 5273），也能验**解压后的桌面版产物**（8765）。
- 新增 `.tools/test-desktop-reuse.py`：盯住上面那条实例复用逻辑的两条分支
  （同版本→复用；版本不同→另起端口且不把用户带去旧界面）。这个坑一旦回归，
  现象只是"界面是旧的"，不会有任何报错。

---

## v0.8.0 · 2026-09-14 · 微信引流 + 版本归档台账

### 新增
- **微信引流入口**：`frontend/src/data/wechat.js` 集中管理二维码与有效期。
  图片走 `import` 引入而不是放 `public/`——**单文件版是双击打开的本地 HTML，
  没有服务器托管 `/wechat/*.jpg`，放 public 会裂图**；走 import 后网站版出独立资源、
  单文件版自动 base64 内联，两条通道都不掉图。
- **四处引流入口**：侧边栏底部「加微信 / 交流群」、首页作者区按钮、设置页「关于」、
  题库页「怎么获取私有题库」。前两处开全局弹层（`components/WeChatHost.vue`，
  支持 ESC 关闭），后两处直接摊开二维码——那两个位置用户本来就在找联系方式，不必多点一次。
- **群二维码到期自动降级**：群码 7 天一轮，`groupExpireAt` 到点后前端自动换成
  「群二维码已过期，加微信拉你进群」。**不展示一张扫了没用的图**，比不展示更伤信任。
  个人微信码长期有效，是主入口；群码只当附加福利。
- **`release/版本说明.md`**：本机维护的分发台账，逐版本记录「有什么功能 / 哪个产物能发给别人」。
  刻意不进版本库——里面写明哪些产物含私有卷，是只给作者看的信息。

### 变更
- **release/ 归档策略：只增不删**。历史版本产物一律保留、互不覆盖（产物名本就带版本号），
  需要回溯"某版当时长什么样"时不用重新构建。
  > v0.4.0 / v0.5.0 的桌面版包是真题分层**之前**打的，内含 2022 年起私有卷，
  > 属泄漏风险，已于 v0.7.0 时清理，不在归档之列。

---

## v0.7.0 · 2026-09-14 · 评分内核补齐：采分点标准层 + 硬规则引擎

### 新增
- **采分点标准层（grading_standard）**：`frontend/src/agents/grading/standard.js` +
  `standardResolver.js`。给题目预先备一份采分点标准（含分值、材料原文依据、判据关键词），
  批改时注入每位老师的提示词，老师有了共同锚点，评分可比、可复算。
  **有标准更好，没有标准照旧裸判**——不会因为一道题没录标准就罢工。
- **硬规则引擎**：`frontend/src/utils/grading/rules.js`。字数 / 标点格式 / 结构分条 /
  重复与照抄四类校验，**纯代码、零依赖、可复算**，不交给模型"感觉"。
  客观扣分默认封顶为满分的 20%，且只作旁证，不直接改写 AI 分数。
- **标准数据分层**：公开卷与仿真题的标准在 `frontend/src/data/standards/`（进仓库）；
  私有卷的标准在 `data/standards-private/`（**.gitignore**，走 `@private-standards` 别名）——
  写出采分点等于泄题，必须和私有卷正文一起隔离。
- **采分点预解析工具**：`.tools/standards/gen_standards.mjs`。批量 LLM 提取采分点与证据串，
  产出落 `data/standards/generated.js`（公开）与 `data/standards-private/index.js`（私有）；
  `--mock` 可在没有 Key 时跑通整条链路，`--limit`/`--ids` 支持小步试跑。
- **练习页两个新面板**：「客观校验」（列出字数/格式/结构/重复问题与改法）与
  「采分点对照」（覆盖率 + 逐点命中/部分/缺失 + 材料依据）。
- 新增两套纯代码单测：`.tools/test-rules.mjs`（24 组断言）、
  `.tools/test-standards.mjs`（17 组断言），以及面板渲染验证 `.tools/probe-standard-panel.mjs`。
- `LICENSE` 之外的采分点样板：`data/standards/public.js` 收录 1 道**人工精校**的样板
  （数字乡村建设），作为格式参照与质量基线。

### 优化
- 硬规则的客观结论会作为「客观事实」注入圆桌辩论与合议的输入，
  避免"字数明显不足""整段照抄材料"这类板上钉钉的问题在合议里无人提及。
- 深度模式的方法论注入顺序调整：先给判断尺度、再给该题采分点锚点。

### 修复
- 仿真题的采分点标准键名统一为**练习页使用的题目 id**（`builtin-q-01`），
  与数据文件里的原始 id（`q-01`）区分——写错键名不会报错，只会静默地永远取不到标准。

---

## v0.6.0 · 2026-09-14 · 作者署名 + 真题分层（公开 2021 及以前，私有 2022 起）

### 新增
- **作者署名体系**：`frontend/src/data/author.js` 是全项目唯一来源（作者 许一 / 邮箱 / GitHub / Gitee /
  开源地址 / 许可）。侧边栏底部、首页页脚、设置页「关于」、浏览器控制台横幅统一从它取；
  `.tools/add_watermark.py` 给 16 个核心源文件打作者注释头，改一次全项目跟着变。
- **侧边栏底部开源入口**：GitHub / Gitee / 更新日志三个入口移到左侧边栏底部常驻
  （原先只在首页页脚，别的页面够不着），侧边栏最下方另有作者署名一行。
- **私有题库包（.bpq）**：作者侧用 `.tools/exams/export_bpq.py` 导出带使用者水印的题库包，
  题库页「导入题库包」一键入库（材料与参考答案都在包里，导入即可练）；
  `.tools/verify-bpq.mjs` 用前端同一份算法在发包前自检。
- **真题分层**：2021 年及以前的国考卷（24 套 / 119 题）随包分发；
  2022 年起的（9 套 / 45 题）为私有资产，**不进版本库、不进分发产物**，由作者定向分发后导入。
- 仓库根 `LICENSE`（AGPL-3.0）。

### 变更
- 真题数据拆成 `frontend/src/data/real-exams/`（公开）与 `real-exams-private/`（私有，已 gitignore）两处；
  只有 `--mode full` / `--mode single-full` 才把私有卷编进去，**默认构建一律不含私有卷** ——
  忘记加 mode 时宁可少东西，也不能把私有资产漏出去。
- 首页页脚改为「作者与开源」信息块（GitHub / Gitee / 日志三个入口已移到侧边栏）。
- 题库页「按来源」筛选新增「私有」；练习页题目区新增来源标签（真题 / 私有 / 仿真 / 自建）。

### 优化
- 题库页说明按公开 / 私有分层重写；未导入私有卷时给出获取途径（作者联系方式可直接复制）。

### 修复
- 练习页与首页把库里所有题目一律标成「自建」，会把导入的私有真题认错来源；
  改为只在缺失时补默认值，不覆盖已有 `kind`。

---

## v0.5.0 · 2026-09-14 · 33 套国考真题入库（2010–2024）

### 新增
- **真题题库**：33 套国考申论卷 / 164 道题（2010–2024，副省级 / 地市级 / 省部级 / 行政执法），
  含完整给定资料与参考答案。题库页可按「真题 / 仿真 / 自建」筛选，卡片标出年份与卷别
- **真题数据流水线**（`.tools/exams/`）：PDF → 分题 → 结构化 → 质量闸门 → 前端数据。
  六道闸门（材料非空 / 题干·要求·分值·字数非空 / 分值合计 100 / 答案齐全 / schema）
  **33 套全部通过**，正文体检 0 处可疑
- 真题**按套懒加载**：题库列表只带摘要，练到哪题才载入该卷正文。
  网站版每套约 15KB（gzip），单文件版自动内联，离线双击仍然可用

### 优化
- 题库页列表按年份从新到旧排列，真题排在仿真题之前（先练近年真题）
- 「参考答案」在题库页按需展开载入，不必提前读完整卷材料
- 题型自动归类覆盖五种题型，「其他」从 42 道降到 0 道

### 修复
- 一题多小问时，「（N分）」与「要求：」之间的**子问题正文被丢弃**（2021 副省级第 3 题）
- 2016 地市级第三题两个小问各自的字数要求与分值被合并后丢失（现各自保留、字数取和）
- 末题要求尾部混入「…卷（解析）」这类答案页眉
- 源 PDF 的 17 处 OCR 错字与引号错位（如 `行政i款`→`行政罚款`、`关于s市`→`关于S市`），
  统一收进 `extract.py` 的 `FIXUPS` 修正表，重跑即复现，不做手工改 JSON

## v0.4.0 · 2026-09-14 · 更新日志文档化，发布产物带版本号

### 新增
- 更新日志改由本文件驱动：网页「更新日志」页直接解析这份 Markdown，改文档即改页面
- 发布产物统一带版本号（`蓝笔申论-单文件版-v0.4.0.html`、`蓝笔申论-桌面版-v0.4.0.zip`）
- 新增 `npm run release:single`，一次完成单文件版构建、改名、清理旧版本

### 优化
- 首页的仓库入口与更新日志入口，从标题旁下移到页面底部，放在配置提示之上

### 变更
- 版本号的唯一来源改为本文件最上面一版，前后端不再各写一份
- `release/` 只保留最新一版产物，重打包时自动清掉旧版本副本

## v0.3.0 · 2026-09-14 · 首页入口 + 更新日志

### 新增
- 更新日志页，按版本记录每轮改动
- 首页 GitHub / Gitee 仓库入口

### 优化
- 关于信息统一走站点元数据，版本号不再散落各处

## v0.2.0 · 2026-09-11 · 界面重构 + 题库扩充 + 每日一练

### 新增
- 每日一练：按当天日期散列选卷，同一天恒定、相邻日期不连号
- 题库扩充至 15 道完整题，五种题型各 3 道，含完整材料与参考答案
- 练习页自动载入今日一练，支持「换一题」从池中重抽
- 方格纸作答区，格宽随容器实测宽度自适应，折行严丝合缝
- 设置页状态总览：通道 / 模型 / Key 来源三栏一眼看清
- 阅读态材料：给定资料按「材料N」分则折叠，长材料不再刷屏

### 优化
- 导航改为左侧分组侧边栏（学习 / 内容 / 实战 / 复盘）
- 练习页左栏空态改为可搜索的选题面板，不再是大片留白

### 变更
- 界面风格换肤：新拟态 → 自然有机风，颜色收敛为语义 token

### 修复
- 修复从题库进入练习会丢失作答要求、分值、字数限制
- 修复桌面版被误判为「未配置 API」，导致批改按钮点不动

### 删除
- 移除 Arco Design 依赖，改自实现轻量提示框（CSS 405KB → 24KB）

## v0.1.0 · 2026-09-08 · 首个可用版本

### 新增
- 五位老师独立阅卷，可按 1~5 位自由组合
- 圆桌分歧复核与加权合议，分歧超阈值才触发复核
- 逐句批注：按老师颜色在作答原文上标出问题句
- 双通道：后端转发与浏览器直连自动降级
- 练习记录本地归档，可跨浏览器复盘
- 单文件 HTML 与 Windows 桌面版两种分发形态
- 老师讲义库与官媒时评文章库
